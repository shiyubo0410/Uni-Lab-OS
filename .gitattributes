# Auto detect text files and perform LF normalization
* text=auto

# Linux/Unix-only files: must use LF, never CRLF
sudoers.d.* text eol=lf
*.sh        text eol=lf
*.service   text eol=lf
*.conf      text eol=lf
unilab-gateway.env* text eol=lf
