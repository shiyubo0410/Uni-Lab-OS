# 香橙派 IoT 网关部署指南

> 把香橙派 Zero 2W 变成 Uni-Lab-OS 实验室仪器接入网关：USB 转 RS-232/RS-485 接到任意串口仪器，网关自动上报数据到 Uni-Lab 云端，前端可实时查看与控制。
>
> 本指南假定你已有：可用的 Uni-Lab 云后端（如 `leap-lab.test.bohrium.com`）、对应的 ak/sk、要挂载的 mount_uuid。

---

## 目录

- [0. 物料清单](#0-物料清单)
- [1. 系统烧录与首次启动](#1-系统烧录与首次启动)
- [2. SSH 连接到香橙派（Windows 端）](#2-ssh-连接到香橙派windows-端)
- [3. 串口与权限检查](#3-串口与权限检查)
- [4. Python 环境准备](#4-python-环境准备)
- [5. 把代码传到香橙派](#5-把代码传到香橙派)
- [6. 创建虚拟环境与安装依赖](#6-创建虚拟环境与安装依赖)
- [7. 配置网关与设备清单](#7-配置网关与设备清单)
- [8. 手动启动测试（先跑通再上 systemd）](#8-手动启动测试先跑通再上-systemd)
- [9. 设置开机自启（systemd）](#9-设置开机自启systemd)
- [10. 日常运维](#10-日常运维)
- [11. 故障处理](#11-故障处理)
- [12. USB 热插拔自动识别（指纹库）](#12-usb-热插拔自动识别指纹库)
- [13. WiFi 配网向导（无显示器开箱配网）](#13-wifi-配网向导无显示器开箱配网)
- [14. A/B 双分区 + OTA 远程升级](#14-ab-双分区--ota-远程升级)
- [15. OTA Agent：TB MQTT 最小闭环（D2 阶段）](#15-ota-agenttb-mqtt-最小闭环d2-阶段)
- [16. 量产母机克隆指南（出厂前必读）](#16-量产母机克隆指南出厂前必读)
- [附录 A：完整 gateway.yaml 示例](#附录-a完整-gatewayyaml-示例)
- [附录 B：网关代码结构](#附录-b网关代码结构)
- [附录 C：文件位置速查表](#附录-c文件位置速查表)

---

## 0. 物料清单

| 物品 | 规格 | 数量 | 备注 |
|---|---|---|---|
| 香橙派 Zero 2W | 1GB RAM | 1 | 主控板 |
| TF 卡 | 16GB+ Class10/工业级 | 1 | 存系统 |
| USB-C 电源 | 5V/2A | 1 | 板子供电 |
| USB-C 读卡器 | - | 1 | 烧录用 |
| USB 转 RS-485 适配器 | CH340/FT232 | 1 | 接 RS-485 仪器 |
| RS-485 接线 | 2 芯（A/B） | 适量 | 视仪器接口 |
| 仪器 | 例：兰格 SY-03B 注射泵 | 1 | 调试目标 |

电脑端：Windows 10/11 + PowerShell 即可。

---

## 1. 系统烧录与首次启动

### 1.1 烧录 Armbian Bookworm

1. 下载 **Orange Pi Zero 2W 的 Armbian Bookworm 镜像**（[官方下载页](http://www.orangepi.org/html/hardWare/computerAndMicrocontrollers/service-and-support/Orange-Pi-Zero-2w.html)）
2. 用 **balenaEtcher** 或 **Rufus** 把镜像烧到 TF 卡
3. 烧完后 TF 卡插到香橙派背面的卡槽

### 1.2 首次启动 + 配网

**有显示器的情况**：
- HDMI 接显示器 + USB 键盘 → 上电 → 第一次会自动扩容 + 重启
- 默认账号 `orangepi` / 默认密码 `orangepi`（首次登录会强制改密）
- 配 WiFi：`sudo nmtui` → Activate a connection → 选你的 WiFi 输密码

**无显示器的情况（推荐）**：
- 烧录后挂载 TF 卡的 boot 分区，编辑 `armbian_first_run.txt`，把 WiFi SSID/密码填进去（或者直接用网线插路由器）
- 上电后等 1-2 分钟，去路由器后台找到香橙派的 IP

### 1.3 找到香橙派 IP

在路由器后台找 `orangepizero2w` 主机名对应的 IP；或者从同网段 Windows 上扫：

```powershell
arp -a | findstr "dynamic"
```

记下 IP，下文都假定为 `10.20.31.230`，**请替换成你自己的**。

---

## 2. SSH 连接到香橙派（Windows 端）

```powershell
ssh orangepi@10.20.31.230
```

第一次会问 `Are you sure you want to continue connecting (yes/no/[fingerprint])?`，输 `yes` 回车，再输 Orange Pi 密码。

### 2.1（可选）配置 SSH 免密登录

避免每次 ssh/scp 都输密码：

```powershell
# 1. 在 Windows 上生成密钥（已经有就跳过）
ssh-keygen -t ed25519
# 一路回车，passphrase 留空

# 2. 把公钥推到香橙派（输一次密码）
type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh orangepi@10.20.31.230 "mkdir -p ~/.ssh && chmod 700 ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys"
```

之后 `ssh orangepi@10.20.31.230` 直接进，不用密码。

---

## 3. 串口与权限检查

把 USB-RS485 适配器插到香橙派，再 SSH 进去：

```bash
# 看到 /dev/ttyUSB0 说明 OS 已识别
ls -l /dev/ttyUSB*
# 期望输出（注意 group 是 dialout）：
# crw-rw---- 1 root dialout 188, 0 ... /dev/ttyUSB0

# 看 orangepi 用户在不在 dialout 组里
groups orangepi
# 输出里应该包含 dialout（Armbian 默认就有）
```

如果 `orangepi` 不在 `dialout` 组，加进去并重新登录：

```bash
sudo usermod -aG dialout orangepi
exit  # 退出 SSH 重新登录才生效
```

---

## 4. Python 环境准备

Armbian Bookworm 自带 Python 3.11，但默认没装 venv 和 pip 工具：

```bash
sudo apt update
sudo apt install -y python3.11-venv python3-full python3-pip git
```

### 4.1（可选）安装 uv 加速 pip

`uv` 比 pip 快几倍，强烈建议：

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.bashrc
uv --version  # 验证
```

### 4.2 配置国内 pip 镜像

```bash
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
pip config set install.trusted-host pypi.tuna.tsinghua.edu.cn

# uv 也用清华源
echo 'export UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple' >> ~/.bashrc
source ~/.bashrc
```

---

## 5. 把代码传到香橙派

> ⚠ 直接在香橙派 `git clone github.com/deepmodeling/Uni-Lab-OS` 经常因网络抖动 RPC failed。**推荐：在 Windows 上 clone 好，再用 scp 推过去**。

### 5.1 Windows 端 clone

```powershell
cd C:\Users\dp1
git clone --depth=1 https://github.com/deepmodeling/Uni-Lab-OS.git
# 国内卡的话用 kkgithub 镜像：
# git clone --depth=1 https://kkgithub.com/deepmodeling/Uni-Lab-OS.git
```

### 5.2 用 scp 推到香橙派

```powershell
ssh orangepi@10.20.31.230 "mkdir -p ~/code"
scp -r C:\Users\dp1\Uni-Lab-OS orangepi@10.20.31.230:~/code/
```

> 大约 3-5 分钟，看网速。完成后香橙派上 `~/code/Uni-Lab-OS/` 就是完整仓库。

---

## 6. 创建虚拟环境与安装依赖

SSH 进香橙派：

```bash
cd ~/code/Uni-Lab-OS
python3 -m venv .venv
source .venv/bin/activate
python --version              # 期望 Python 3.11.x
which python                  # 期望 ~/.venv/bin/python
```

### 6.1 安装 Uni-Lab-OS 主包（不拉一堆 ROS 2 依赖）

```bash
pip install -e . --no-deps    # 仅安装包元数据，让 import 路径生效
```

### 6.2 安装网关运行时依赖（最小集）

```bash
# 用 uv 装更快，没装 uv 就把 uv 改成 pip
uv pip install pyserial pymodbus websockets pyyaml requests
```

### 6.3 验证 import

```bash
python -c "import unilabos; import serial, pymodbus, websockets, yaml, requests; print('OK')"
# 输出 OK 说明环境搭好了
```

### 6.4（重要）让 SSH 重登录后自动激活 venv

```bash
echo 'source ~/code/Uni-Lab-OS/.venv/bin/activate' >> ~/.bashrc
```

下次 SSH 进来不用每次手动 `source`。

---

## 7. 配置网关与设备清单

### 7.1 写 `~/gateway.yaml`

```bash
nano ~/gateway.yaml
```

最小可用示例（接一台兰格 SY-03B 注射泵，USB 转 RS-485）：

```yaml
# Uni-Lab IoT 网关配置
machine_name: gw-zero2w-001     # 网关唯一名字，建议 gw-<板型>-<编号>

devices:
  - device_id: pump-sy03b-001                                    # 设备 ID（前端会显示这个）
    name: SY-03B 注射泵
    driver: unilabos.devices.pump_and_valve.runze_backbone.RunzeSyringePump
    registry_class: syringe_pump_with_valve.runze.SY03B-T06      # 必填，对应云端 registry 里的设备类名
    init:
      port: /dev/ttyUSB0
      address: "1"
      max_volume: 25.0
    properties:                                                  # 周期性轮询的属性
      - status
      - position
      - valve_position
    poll_interval: 1.5                                           # 轮询间隔（秒）
    report_unchanged: false                                      # 值没变就不重复上报，省带宽

    # 稳定性参数（避免设备异常时卡死整个网关）
    property_timeout: 5.0       # 单次属性读超时
    action_timeout: 120.0       # 单次动作执行超时（initialize 类动作可能需要几十秒）
```

> 完整字段说明见 [附录 A](#附录-a完整-gatewayyaml-示例)。

### 7.2 找设备的 driver 路径与 registry_class

- **driver**：在 Uni-Lab-OS 仓库 `unilabos/devices/` 下找对应的 Python 类，写完整路径
- **registry_class**：在 `unilabos/registry/devices/*.yaml` 里找对应设备的 `id` 字段。**这个值必须和云端 registry 里的设备类名完全一致**，否则 HTTP 注册会被后端拒绝（报 `class invalid`）

例如 SY-03B：
- 文件 `unilabos/registry/devices/pump_and_valve.yaml` 中能找到 `id: syringe_pump_with_valve.runze.SY03B-T06`
- 对应驱动 `unilabos/devices/pump_and_valve/runze_backbone.py` → 类 `RunzeSyringePump`

如果找不到对应的 `registry_class`，先暂时填 `host_node`（万能 fallback，前端能看到设备但是没有 action 模板）。

### 7.3 验证驱动能直接通信（独立于网关）

在挂上网关之前，先确认驱动能正确读到设备：

```bash
python -c "from unilabos.devices.pump_and_valve.runze_backbone import RunzeSyringePump; p = RunzeSyringePump(port='/dev/ttyUSB0', address='1', max_volume=25.0); print('status:', p.get_status()); print('position:', p.get_position()); print('valve:', p.get_valve_position())"
```

期望输出：

```
status: Idle
position: 0.0
valve: ?         （首次未初始化为 ?，正常）
```

如果卡住或抛 `RunzeSyringePumpConnectionError`：
- 检查接线（A/B 别接反；GND 也接上）
- 检查设备地址是否真的是 `1`
- 检查 RS-485 适配器是否需要切换 A/B 拨码
- 给设备**断电 5 秒再上电**，重试

---

## 8. 手动启动测试（先跑通再上 systemd）

### 8.1 启动命令（命令行参数全展开）

```bash
cd ~/code/Uni-Lab-OS && source .venv/bin/activate

python -m unilabos.gateway.main \
  --config ~/gateway.yaml \
  --ws-url wss://leap-lab.test.bohrium.com/api/v1/ws/schedule \
  --http-url https://leap-lab.test.bohrium.com/api/v1 \
  --register \
  --mount-uuid 5d2230c3-b978-4635-bfd8-c9486556a005 \
  --ak 567ac3d4-7bee-4bfb-908a-91552164dd7e \
  --sk ae7cf17f-8aae-4005-96e9-5086e84c9324 \
  --log-level INFO
```

字段说明：

| 参数 | 含义 | 怎么获取 |
|---|---|---|
| `--ws-url` | WebSocket 端点 | 测试环境：`wss://leap-lab.test.bohrium.com/api/v1/ws/schedule`；生产换 host |
| `--http-url` | HTTP API base | 同上，把 `wss` 换成 `https`，路径 `/api/v1` |
| `--register` | 启动时调 HTTP `/edge/material` 注册资源树 | 第一次必带，前端才能看到设备 |
| `--mount-uuid` | 把网关挂在哪个父节点下 | 在前端"实验室"节点上看 UUID，或问管理员 |
| `--ak` / `--sk` | 认证密钥 | 实验室管理员发放 |

### 8.2 期望日志

```
[GW] 启动: machine_name=gw-zero2w-001 devices=1 ws=wss://...
[REG] 注册 host_node 到 ... 
[REG] host_node 注册成功: cloud_uuid=...
[REG]   注册设备 pump-sy03b-001 class=syringe_pump_with_valve.runze.SY03B-T06 parent=...
[REG] 完成: host_node + 设备 1/1 注册成功
[GW] 已加载设备 pump-sy03b-001 -> unilabos.devices.pump_and_valve.runze_backbone.RunzeSyringePump
[DEV] pump-sy03b-001 启动 properties=['status', 'position', 'valve_position'] interval=1.5s
[DEV] pump-sy03b-001 status = 'Idle'
[GW] 已连接 wss://... session=...
[GW] host_node_ready: 上报 1 个设备
[DEV] pump-sy03b-001 position = 0.0
[DEV] pump-sy03b-001 valve_position = '?'
```

### 8.3 在前端验证

打开 Uni-Lab 前端 → 找到挂载点（mount_uuid 对应的实验室）→ 应该看到：

- 网关节点 `gw-zero2w-001`
- 子节点 `pump-sy03b-001`
- 点击设备 → 实时面板有 `status / position / valve_position` 三个属性更新
- 点击设备的 "auto-initialize" 按钮 → 网关日志会出现 `job_start`，泵会动

`Ctrl+C` 停掉手动测试，进入 systemd 部署。

---

## 9. 设置开机自启（systemd）

### 9.1 创建环境变量文件 `/etc/unilab-gateway.env`

```bash
sudo nano /etc/unilab-gateway.env
```

内容（**把 ak/sk/url/mount_uuid 换成你的**）：

```bash
WS_URL=wss://leap-lab.test.bohrium.com/api/v1/ws/schedule
HTTP_URL=https://leap-lab.test.bohrium.com/api/v1
MOUNT_UUID=5d2230c3-b978-4635-bfd8-c9486556a005
UNILABOS_BASICCONFIG_AK=567ac3d4-7bee-4bfb-908a-91552164dd7e
UNILABOS_BASICCONFIG_SK=ae7cf17f-8aae-4005-96e9-5086e84c9324
```

设置只有 root 可读（**重要：里面是密钥**）：

```bash
sudo chmod 600 /etc/unilab-gateway.env
```

### 9.2 创建 systemd 服务 `/etc/systemd/system/unilab-gateway.service`

```bash
sudo nano /etc/systemd/system/unilab-gateway.service
```

内容（**注意 ExecStart 是一整行，路径替换成你自己的**）：

```ini
[Unit]
Description=Uni-Lab IoT Gateway
Documentation=https://github.com/deepmodeling/Uni-Lab-OS
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=orangepi
Group=dialout
WorkingDirectory=/home/orangepi/code/Uni-Lab-OS
EnvironmentFile=/etc/unilab-gateway.env
ExecStart=/home/orangepi/code/Uni-Lab-OS/.venv/bin/python -m unilabos.gateway.main --config /home/orangepi/gateway.yaml --ws-url ${WS_URL} --http-url ${HTTP_URL} --register --mount-uuid ${MOUNT_UUID} --ak ${UNILABOS_BASICCONFIG_AK} --sk ${UNILABOS_BASICCONFIG_SK} --log-level INFO

Restart=always
RestartSec=5s
StandardOutput=journal
StandardError=journal
SyslogIdentifier=unilab-gateway

[Install]
WantedBy=multi-user.target
```

### 9.3 启用与启动

```bash
sudo systemctl daemon-reload
sudo systemctl enable unilab-gateway     # 开机自启
sudo systemctl start unilab-gateway      # 立即启动
sudo systemctl status unilab-gateway --no-pager | head -15
```

看到 `active (running)` 就成功了。

### 9.4 跟踪日志

```bash
journalctl -u unilab-gateway -f
# Ctrl+C 退出
```

### 9.5 重启大法测试

```bash
sudo reboot
# 重启后等 30 秒，再 SSH 进来
sudo systemctl is-active unilab-gateway   # 期望输出 active
```

---

## 10. 日常运维

### 10.1 修改 ak/sk 或云端 URL

```bash
sudo nano /etc/unilab-gateway.env
sudo systemctl restart unilab-gateway
journalctl -u unilab-gateway -n 30 --no-pager
```

### 10.2 修改设备清单

```bash
nano ~/gateway.yaml
sudo systemctl restart unilab-gateway
```

### 10.3 推送代码更新（Windows → 香橙派）

修改了 `unilabos/gateway/*.py` 后：

```powershell
# 推单个文件
scp D:\Uni-Lab\Uni-Lab-OS\unilabos\gateway\device.py orangepi@10.20.31.230:~/code/Uni-Lab-OS/unilabos/gateway/device.py

# 推整个 gateway 目录
scp -r D:\Uni-Lab\Uni-Lab-OS\unilabos\gateway orangepi@10.20.31.230:~/code/Uni-Lab-OS/unilabos/

# 推完后在香橙派上
sudo systemctl restart unilab-gateway
```

### 10.4 常用诊断命令

```bash
# 服务是否在跑
sudo systemctl is-active unilab-gateway

# 完整状态（含最近 10 行日志）
sudo systemctl status unilab-gateway --no-pager

# 实时日志
journalctl -u unilab-gateway -f

# 最近 100 行
journalctl -u unilab-gateway -n 100 --no-pager

# 只看错误
journalctl -u unilab-gateway -p err --since "1 hour ago"

# 看当前 USB 串口
ls -l /dev/ttyUSB*

# 看 env 配置（确认 ak/sk 改对没）
sudo cat /etc/unilab-gateway.env
```

### 10.5 临时调试（绕过 systemd）

需要前台跑、看更详细日志：

```bash
sudo systemctl stop unilab-gateway
cd ~/code/Uni-Lab-OS && source .venv/bin/activate
python -m unilabos.gateway.main --config ~/gateway.yaml \
  --ws-url wss://leap-lab.test.bohrium.com/api/v1/ws/schedule \
  --http-url https://leap-lab.test.bohrium.com/api/v1 \
  --register --mount-uuid 5d2230c3-b978-4635-bfd8-c9486556a005 \
  --ak <你的ak> --sk <你的sk> \
  --log-level DEBUG     # ← 看 WebSocket 收发详情

# 调试完恢复 systemd
sudo systemctl start unilab-gateway
```

---

## 11. 故障处理

### 11.1 服务起不来（`systemctl status` 显示 `failed`）

```bash
journalctl -u unilab-gateway -n 50 --no-pager
```

常见错误：

| 报错 | 原因 | 解决 |
|---|---|---|
| `握手失败 status=401` | ak/sk 错或不属于这个环境 | 检查 `/etc/unilab-gateway.env` |
| `握手失败 status=200` | URL 不对（默认到了生产环境，但 ak/sk 是测试的） | 显式指定 `--ws-url`/`WS_URL` |
| `[REG] POST 返回非 0: code=22000` | `class invalid`：`registry_class` 不在云端 registry | 改成 `host_node` 或正确的 class |
| `Permission denied: '/dev/ttyUSB0'` | orangepi 不在 dialout 组 | `sudo usermod -aG dialout orangepi` 后重新登录 |
| `ModuleNotFoundError: serial` | venv 没激活或没装依赖 | `source .venv/bin/activate && uv pip install pyserial` |

### 11.2 设备无响应（持续超时）

```
[DEV] 读取 pump-sy03b-001.status 超时（>5.0s），已连续 1 次。检查设备电源/接线/波特率
```

按以下顺序排查：

1. **物理重启设备**（拔泵的电源 5 秒，再上电）—— 最常见，70% 的问题这步能解
2. **物理重新插拔 USB-RS485 适配器**
3. **检查 A/B 接线没反，GND 也接上**
4. **直接跑驱动测试**（绕过网关）：
   ```bash
   sudo systemctl stop unilab-gateway
   cd ~/code/Uni-Lab-OS && source .venv/bin/activate
   python -c "from unilabos.devices.pump_and_valve.runze_backbone import RunzeSyringePump; p = RunzeSyringePump(port='/dev/ttyUSB0', address='1', max_volume=25.0); print('status:', p.get_status())"
   ```
   - 如果这条命令也卡住或抛错，**就是设备/接线问题**，不是网关问题
   - 能拿到 `Idle` 就 `sudo systemctl start unilab-gateway` 恢复服务

### 11.3 USB 拔插或设备断电后

网关有自动重连机制（**约 1 分钟内自动恢复**）：

- 累计 9 次连续 IO 错误（约 45-50 秒）→ 触发驱动重建
- 每 ~15 秒尝试一轮重建，直到 driver 重连成功
- 重连成功后 telemetry 自动恢复

如果**超过 2 分钟还在持续超时**，多半是设备真的坏了或接线松了，按 11.2 处理。

### 11.4 前端看不到设备

1. 服务在跑：`sudo systemctl is-active unilab-gateway` → `active`
2. WebSocket 连上：日志里有 `[GW] 已连接 wss://...`
3. 资源树注册了：日志里有 `[REG] 完成: host_node + 设备 X/X 注册成功`
4. 前端实验室是不是同一个 mount_uuid？查 `/etc/unilab-gateway.env` 的 `MOUNT_UUID`
5. ak/sk 对应的账号有没有访问权限？

### 11.5 ttyUSB 编号变了（重启后变成 ttyUSB1）

如果系统接了多个 USB 串口设备，`/dev/ttyUSB0` 可能变成 `/dev/ttyUSB1`。解决：用 udev 规则绑定固定名字。

```bash
# 看适配器的 VID:PID
udevadm info -a -n /dev/ttyUSB0 | grep -E "idVendor|idProduct|serial" | head -5

# 例如 FTDI：idVendor="0403" idProduct="6001" serial="ABCD1234"
sudo nano /etc/udev/rules.d/99-unilab-serial.rules
```

写入：

```
# 把这个 USB-RS485 适配器固定为 /dev/ttyUSB-RS485
SUBSYSTEM=="tty", ATTRS{idVendor}=="0403", ATTRS{idProduct}=="6001", ATTRS{serial}=="ABCD1234", SYMLINK+="ttyUSB-RS485", GROUP="dialout", MODE="0660"
```

应用：

```bash
sudo udevadm control --reload-rules
sudo udevadm trigger
ls -l /dev/ttyUSB-RS485
```

`gateway.yaml` 里把 `port: /dev/ttyUSB0` 改成 `port: /dev/ttyUSB-RS485`。

---

## 12. USB 热插拔自动识别（指纹库）

> **目标**：用户即使不写 `gateway.yaml` 里的 `devices`，把已知设备（注射泵、电导传感器等）插上 USB 也能自动出现在前端。仓库内置的 *指纹库* 会探测设备型号并自动注册。

### 12.1 工作原理

```
USB 设备插入
  ↓ (内核加载 ttyUSB 节点)
udev 规则触发 hotplug_handler
  ↓ (调驱动尝试通信，匹配指纹)
写入信号文件 /tmp/unilab-gateway/hotplug/<device>.json
  ↓
unilab-gateway 主进程检测到信号文件
  ↓
DeviceWorker 启动 + HTTP 注册到云端
  ↓
前端自动出现新设备
```

涉及的核心文件：

- `unilabos/gateway/discovery/scanner.py` — 主动扫描（启动时）
- `unilabos/gateway/discovery/probe.py` — 单端口指纹探测
- `unilabos/gateway/discovery/hotplug_handler.py` — udev 触发的入口脚本
- `unilabos/gateway/discovery/99-unilab-gateway.rules` — udev 规则
- `unilabos/gateway/main.py:_hotplug_monitor()` — 主进程信号文件监听

### 12.2 安装 udev 规则（首次部署一次性）

```bash
# 复制规则到系统目录
sudo cp ~/code/Uni-Lab-OS/unilabos/gateway/discovery/99-unilab-gateway.rules /etc/udev/rules.d/

# 让 udev 重新加载规则
sudo udevadm control --reload-rules
sudo udevadm trigger

# 创建信号文件目录（systemd 服务会自动建，这里手动确认一次）
sudo mkdir -p /tmp/unilab-gateway/hotplug
sudo chown orangepi:orangepi /tmp/unilab-gateway/hotplug
```

规则覆盖了 FTDI / CH340 / CP210x / 通用 USB CDC ACM / `ttyUSB*` / `ttyACM*`。

### 12.3 启动时主动扫描（可选）

systemd 命令里给 `python -m unilabos.gateway.main` 加 `--auto-discover`，启动时会扫一遍当前所有串口，把识别到的设备一次性合并到 `devices` 列表：

```ini
ExecStart=/home/orangepi/code/Uni-Lab-OS/.venv/bin/python -m unilabos.gateway.main \
    --config /home/orangepi/gateway.yaml \
    --auto-discover \
    --ws-url ${WS_URL} ...
```

> ⚠ 手动配置 (`gateway.yaml` 中已写的 `device_id`) 优先于自动发现，重名时自动发现的会被跳过。

### 12.4 验证

插上一台兰格 SY-03B 注射泵（或仓库支持的其他设备）：

```bash
# 看 hotplug_handler 的输出
ls /tmp/unilab-gateway/hotplug/
# 期望出现 *.json，例如：runzesyringepump-auto-3f4b.json

# 看主进程日志
journalctl -u unilab-gateway -f
# 期望看到：
# [HOTPLUG] 探测到设备 runzesyringepump-auto-3f4b -> /dev/ttyUSB0
# [DEV] runzesyringepump-auto-3f4b 启动 properties=...
# [REG]   注册设备 runzesyringepump-auto-3f4b ...
```

### 12.5 故障处理

| 现象 | 排查 |
|---|---|
| 插上 USB 没反应 | 1) `lsusb` 看内核是否识别；2) `ls /dev/ttyUSB*` 看 tty 节点；3) `udevadm test $(udevadm info -q path -n /dev/ttyUSB0)` 看规则是否触发 |
| `journalctl -u unilab-gateway` 一直刷 `设备 X 已存在，跳过` | 之前已经被识别过且仍在线，正常调试输出，可忽略 |
| `[HOTPLUG] 探测失败` | 设备未在指纹库中。改成手写 `gateway.yaml` 加上 `registry_class` |
| 信号文件没出现 | 检查 `/tmp/unilab-gateway/hotplug/` 目录权限是 `orangepi` 拥有；udev 规则中的 venv python 路径必须正确 |

---

## 13. WiFi 配网向导（无显示器开箱配网）

> **目标**：用户拿到一台没接显示器的香橙派，掏出手机就能给它配 WiFi，不用 `nmtui`、不用插网线、不用接 HDMI。

### 13.1 工作原理

网关 Web 服务器有两种工作模式，用一份 `ProvisioningServer` 代码切换：

```
[ AP 配网模式 ] mode=provisioning
  开机 → 没保存 WiFi / 连不上 → create_ap 起热点 + Web 在 192.168.12.1:80 提供配网页
  ↓
[ STA 联网模式 ] mode=management
  WiFi 连通后 → Web 在 <网关 LAN IP>:80 常驻管理后台
  → 同 WiFi 下浏览器打开就能改 AK/SK / 重置 WiFi，**不用 ssh + sudo nano /etc/unilab-gateway.env**
```

**完整启动流程**：

```
开机 → unilab-gateway 启动
  ↓
检查是否有已保存的 WiFi 配置 (nmcli connection show)
  ↓ 有且能 ping 公网 → 跳过配网，直接进 STA 模式业务
  ↓ 无 / 连不上
[配网模式]
  1. 启动 create_ap 建立热点 SSID = UniLab-Gateway-<MAC后4位>，密码 = unilab1234
  2. FastAPI 在 192.168.12.1:80 提供配网网页（mode=provisioning）
  ↓
用户手机连 AP → 浏览器打开 192.168.12.1 → 选 SSID + 输密码 → 提交
  ↓
网关把 SSID/密码写入 NetworkManager → 关闭 AP → sudo reboot
  ↓ 重启后 NM autoconnect 上目标 WiFi
[STA 模式]
  1. systemd 拉起 unilab-gateway，连上云端 WS
  2. 同时启动管理后台 Web（mode=management）监听 0.0.0.0:80
  3. 用户日后改 AK/SK / 重置 WiFi 直接浏览器打开 http://<gateway-lan-ip>
```

> ⚠ 为什么配网完成后要 reboot？香橙派 Zero 2W 用的 unisoc `sprdwl_ng` 驱动，在 AP→STA 模式切换时驱动状态机不稳定（rmmod/modprobe / restart NetworkManager 都救不回来）。**唯一稳的方案是配网完成后整机重启**，开机后驱动以 STA 模式干净启动。

涉及的核心文件：

- `unilabos/gateway/provisioning/wifi_manager.py` — `nmcli` + `create_ap` 包装
- `unilabos/gateway/provisioning/web_server.py` — FastAPI 配网/管理网页后端（mode 二合一）
- `unilabos/gateway/provisioning/templates/provision.html` — AP 模式：WiFi 配置页
- `unilabos/gateway/provisioning/templates/management.html` — STA 模式：AK/SK 管理页
- `unilabos/gateway/provisioning/sudoers.d.unilab-gateway` — 免密 sudo 配置
- `unilabos/gateway/main.py:_run_provisioning_mode()` — 配网模式入口
- `unilabos/gateway/main.py:_start_admin_server()` — STA 管理后台启动入口

### 13.2 部署：安装 sudoers 配置

让 `orangepi` 用户能免密 sudo 跑：

- `create_ap` / `nmcli` / `pkill` / `reboot` —— **AP 配网必须**
- `tee` / `cat /etc/unilab-gateway.env` —— **管理后台读写 AK/SK 必须**
- `systemctl restart unilab-gateway` —— **管理后台改完 AK/SK 重启服务必须**

```bash
sudo install -m 440 -o root -g root \
    ~/code/Uni-Lab-OS/unilabos/gateway/provisioning/sudoers.d.unilab-gateway \
    /etc/sudoers.d/unilab-gateway

# 校验语法（必做，写错会让全系统 sudo 失效）
sudo visudo -cf /etc/sudoers.d/unilab-gateway
# 期望输出：/etc/sudoers.d/unilab-gateway: parsed OK

# 验证免密生效
sudo -n -l | grep -E "create_ap|reboot|tee|cat|systemctl"
# 期望看到 create_ap / nmcli / pkill / reboot / tee / cat / systemctl restart 都列出
```

> **从老版本升级**：如果之前装过这份 sudoers（v1.1 之前没有 `tee/cat/systemctl` 三条），直接重新跑上面的 `sudo install ...` 覆盖就行。不更新这几条的话，管理后台改 AK/SK 时会报"写入失败：sudo: a password is required"。

### 13.3 部署：安装 mDNS（让网关有固定网址 `http://unilab-gateway.local`）

**目标**：让用户**不需要查 IP**，永远用同一个网址打开管理后台 —— `http://unilab-gateway.local`。

**工作原理**：mDNS（也叫 Bonjour / zeroconf）是同一局域网内的"小型 DNS"——OPi 通过 UDP 5353 广播自己叫 `unilab-gateway.local`，同一 WiFi 下的浏览器自己就能解析过去。换 WiFi、换 IP，全自动跟随，**用户永远只记一个网址**。

**客户端兼容性**：

- macOS / iOS / Android：原生支持，**零配置**
- Linux / 树莓派 / 其他 OPi：原生支持
- **Windows 10 1809+ / Win11**：原生支持（多数情况开箱即用）
- **老版本 Windows**：装一次 [Bonjour Print Services](https://support.apple.com/kb/DL999)（4 MB，免重启）

#### 13.3.1 设置 OPi 主机名（一次性）

把默认的 `orangepizero2w` 改成产品名 `unilab-gateway`：

```bash
sudo hostnamectl set-hostname unilab-gateway
sudo sed -i 's/orangepizero2w/unilab-gateway/g' /etc/hosts
hostname   # 验证：应输出 unilab-gateway
```

> **多台 OPi 同 WiFi 部署**：mDNS 会自动给重复主机名加后缀（如 `unilab-gateway-2.local`、`unilab-gateway-3.local`）；也可以手动设置不同 hostname，如 `unilab-gateway-A`、`unilab-gateway-B`。

#### 13.3.2 安装并配置 avahi-daemon

```bash
# 1. 装包（avahi-daemon 广播，libnss-mdns 让本机也能解析 .local）
sudo apt update
sudo apt install -y avahi-daemon avahi-utils libnss-mdns

# 2. ⚠ 关键：默认 publish-workstation=no，必须打开才会广播自己
sudo sed -i 's/^publish-workstation=no/publish-workstation=yes/' /etc/avahi/avahi-daemon.conf
sudo sed -i 's/^#publish-workstation=yes/publish-workstation=yes/' /etc/avahi/avahi-daemon.conf
grep "^publish-workstation" /etc/avahi/avahi-daemon.conf
# 期望：publish-workstation=yes

# 3. 注册 _http._tcp 服务（Bonjour Browser 可看到「Uni-Lab Gateway Admin」）
sudo tee /etc/avahi/services/unilab-gateway-admin.service > /dev/null <<'EOF'
<?xml version="1.0" standalone='no'?>
<!DOCTYPE service-group SYSTEM "avahi.dtd">
<service-group>
  <name replace-wildcards="yes">Uni-Lab Gateway Admin (%h)</name>
  <service>
    <type>_http._tcp</type>
    <port>80</port>
    <txt-record>path=/</txt-record>
  </service>
</service-group>
EOF

# 4. 启用并启动 avahi
sudo systemctl enable --now avahi-daemon
sudo systemctl restart avahi-daemon
```

#### 13.3.3 验证

```bash
# 本机自检（OPi 上）
avahi-resolve -n unilab-gateway.local
# 期望：unilab-gateway.local  10.20.31.230

ping -c 2 unilab-gateway.local
# 期望：能 ping 通

# 看自己广播的服务
avahi-browse -alr --terminate 2>/dev/null | grep "Gateway Admin"
# 期望：+ wlan0 IPv4 Uni-Lab Gateway Admin (unilab-gateway)  Web Site  local
```

**客户端验证**：在同 WiFi 下的 Mac / 手机 / 现代 Windows 浏览器打开 `http://unilab-gateway.local` 应直接看到管理后台。

#### 13.3.4 关键坑提示

| 现象 | 原因 | 修复 |
|---|---|---|
| `ping unilab-gateway.local` 报 `Name or service not known` | `libnss-mdns` 没装，glibc 不走 mDNS | `sudo apt install -y libnss-mdns` 后自动改 `/etc/nsswitch.conf` |
| `avahi-resolve -n xxx.local` 也超时 | `publish-workstation=no` 没改 | 见 13.3.2 step 2 |
| `/etc/avahi/services/` 空 | 之前用 `sudo tee` 但 sudoers 未授权该路径 | 直接在 ssh 里输 sudo 密码跑 13.3.2 step 3，**不要**走 sudoers 免密 |
| 老 Windows 浏览器超时 | 没装 mDNS 客户端 | 装 [Bonjour Print Services](https://support.apple.com/kb/DL999)，或换浏览器/换电脑试 |
| `unilab-gateway.local` 解析但浏览器 ERR_CONNECTION_REFUSED | mDNS 通了但管理后台没起 | 看 §13.7.1，查 `journalctl -u unilab-gateway` |

> **mDNS 配置和 WiFi 解耦**：本节做完是**一次性**配置。后续换 WiFi、网关重启、客户端切到新 WiFi —— mDNS 都会自动跟随新 IP 重新广播，用户始终用 `http://unilab-gateway.local` 即可。

### 13.4 部署：确认 create_ap 已安装

Orange Pi 官方镜像通常预装了 create_ap：

```bash
which create_ap
# 期望：/usr/bin/create_ap
create_ap --version
```

如果没有：

```bash
sudo apt install -y util-linux procps iproute2 iw haveged dnsmasq hostapd
git clone https://github.com/oblique/create_ap.git /tmp/create_ap
cd /tmp/create_ap && sudo make install
```

### 13.5 部署：systemd 启用配网模式

`unilab-gateway.service` 默认启动时会自动检测 WiFi。如果你已经手动配好了 WiFi 想完全跳过配网逻辑，给 `ExecStart` 加 `--skip-provisioning`；否则**什么都不用改**。

### 13.6 用户首次开箱配网（产品视角）

1. 把 TF 卡插好、USB-C 通电（不接显示器）
2. **等约 60 秒**让系统启动 + AP 建好
3. 拿手机搜索 WiFi → 看到 `UniLab-Gateway-XX` → 输密码 `unilab1234` 连上
4. 浏览器打开 `192.168.12.1`（iOS 多数会自动弹 captive portal）
5. 在 SSID 下拉里选目标 WiFi（或直接手动输入），输密码，点 **连接并保存**
6. 看到"已收到配网信息，正在切换 WiFi..."提示后约 1 分钟，香橙派会自动重启
7. 重启完成后到 Uni-Lab 控制台看网关是否上线

### 13.7 网关管理后台（不进 ssh 改 AK/SK）

联网正常运行后，网关会**常驻**起一个管理 Web 后台（`mode=management`），让用户从浏览器：

- 修改实验室 AK/SK / mount_uuid / WS_URL / HTTP_URL
- 一键"重置 WiFi"重新进入 AP 配网模式

实测路径，**完全不需要 ssh + `sudo nano /etc/unilab-gateway.env`**。

#### 13.7.1 怎么找到管理后台地址？

管理后台监听**所有网卡的 80 端口**（root 启动）或 **8080 端口**（非 root 启动）。

**首选：mDNS 固定网址 `http://unilab-gateway.local`** —— 同 WiFi 下浏览器直接打开，**用户永不用查 IP**。前提是出厂时做过 §13.3 的 mDNS 部署。多台网关时浏览器看到的就是 `http://unilab-gateway.local` / `http://unilab-gateway-A.local` 之类。

**备选 IP 方法**（mDNS 不可用时，比如客户端不支持 mDNS 或部署没做 §13.3）：

1. **路由器后台 + 静态 IP 绑定**
   - 登路由器后台 → DHCP 客户端列表 → 找到 hostname `unilab-gateway`（或你设置的名字）
   - 给它绑定 MAC → 静态 IP（比如 `192.168.1.200`）
   - 这个 IP 写在网关底部标签 / 客户文档里，永远是它

2. **网关启动日志**
   ```bash
   ssh orangepi@<gw>
   journalctl -u unilab-gateway -n 80 --no-pager | grep -E "管理后台|管理地址"
   ```
   会看到类似：
   ```
   [GW] ============================================================
   [GW] 网关管理后台已启动 (mode=management)
   [GW]   管理地址: http://192.168.1.123
   [GW]   主机名:   unilab-gateway
   [GW]   局域网 IP: 192.168.1.123
   [GW] 同 WiFi 下浏览器打开即可修改 AK/SK / 重置 WiFi
   [GW] ============================================================
   ```

3. **网关本地 `hostname -I`** —— 终端里执行：
   ```bash
   ssh orangepi@<gw> hostname -I
   # 192.168.1.123 ...
   ```

#### 13.7.2 修改 AK/SK（最常用场景）

1. 同一 WiFi 下用浏览器打开 `http://unilab-gateway.local`（或 `http://<网关-LAN-IP>`）
2. 看到顶部"网关管理"页面，状态卡里能看到当前的 AK/SK/mount_uuid（明文）
3. 改成新值 → 点 **保存并重启网关服务**
4. 等约 5 秒页面提示"配置已保存。网关将在 3 秒后重启 unilab-gateway 服务以应用新的 AK/SK"
5. 约 30 秒后到 Uni-Lab 控制台看网关是否上线

> ⚠ 万一新 AK/SK 填错了，网关连不上云端 → 就**再次访问 `http://unilab-gateway.local` 改回正确值**。本管理后台不依赖云端连接。

> ⚠ **切换到全新实验室时**云端可能还没有该实验室的设备/资源 schema，会出现 `class material resource template not exist` 导致设备注册失败。这一步**必须在装了完整 ROS2 + unilabos_msgs 的 PC 上**跑：
> ```bash
> unilab --upload_registry --addr <prod|test|...> --ak <NEW_AK> --sk <NEW_SK>
> ```
> 网关本身不带 ROS2，**跑不了** `upload_registry`。注册表上传是一次性动作——每个实验室只需做一次，之后即使网关在不同实验室之间切换，也不需要重复上传（除非该实验室的设备库被清空过）。

#### 13.7.3 重置 WiFi（换网络）

1. 浏览器打开管理后台 → 滚到底部"⚠ 危险操作"
2. 点 **⟲ 重置 WiFi 配置** → 弹窗确认 → 网关清掉所有已保存的 WiFi connection 并 reboot
3. 重启后回到 AP 配网模式，按 §13.6 重新配网

#### 13.7.4 安全提示

- 当前**没加访问密码**，同 WiFi 内任何人都能改你的 AK/SK
- 部署到不可信 WiFi（公共 / 客户局域网）前，建议在路由器层做 ACL：把 80 端口只开给特定 MAC / IP
- 后续如果有需求再加 HTTP Basic Auth（默认密码贴在网关底部标签）

### 13.8 重新配网（换 WiFi）

**首选：用管理后台 §13.7.3** —— 直接浏览器一键重置，比 ssh 方便。

**备选：ssh 方式**（管理后台连不上时用）：

```bash
ssh orangepi@<当前 IP>

# 列出已保存的 WiFi
nmcli connection show

# 删掉旧的（替换成实际 SSID）
sudo nmcli connection delete <旧 SSID>

# 必须 reboot，让 unisoc 驱动从干净状态进入 AP 模式
sudo reboot
```

重启后系统会重新进入配网模式，按 13.6 第 3 步开始操作即可。

> 为什么必须 reboot？同 13.1 节脚注。删了 WiFi 不重启，下次进配网时 wlan0 可能仍在 STA 半连状态，AP 起不来或者起来了别人搜不到。

### 13.9 故障处理

| 现象 | 排查 |
|---|---|
| 手机搜不到 `UniLab-Gateway-XX` 热点 | 1) SSH 进去看 `journalctl -u unilab-gateway -f` 里 `[AP]` 行；2) `which create_ap` 是否存在；3) `sudo -n create_ap --help` 验证免密；4) 物理重启香橙派 |
| 手机连上 AP 但浏览器打不开 192.168.12.1 | 1) 检查手机连的是不是 `UniLab-Gateway-XX`；2) `ip a show wlan0` 看是不是 `192.168.12.1/24`；3) `curl http://192.168.12.1` 在香橙派本地测试 |
| 提交 WiFi 信息后香橙派一直不上线 | 多半是密码错。等 1-2 分钟，`UniLab-Gateway-XX` 会重新出现，重新连进去重试 |
| AP 起不来报 `Failed to initialize ...` | 之前一次配网没退干净。`sudo pkill -INT create_ap; sudo nmcli dev set wlan0 managed yes; sudo reboot` |
| `journalctl` 里看到 `permission denied` 调 `create_ap` / `reboot` | sudoers 没装好，回到 13.2 重做 |
| 管理后台保存 AK/SK 报"写入失败：sudo: a password is required" | sudoers 老版本，没 `tee`/`systemctl` 免密授权。重做 §13.2，确认 `sudo -n -l | grep tee` 有输出 |
| 管理后台地址打不开 (Connection refused) | 1) 看启动日志 §13.7.1，确认 admin_server 起来了；2) 防火墙放行 80（或 8080）；3) `curl http://localhost` 在网关本地测试 |
| 改完 AK/SK 服务一直没重启 | `sudo -n -l | grep "systemctl restart"` 看是不是 sudoers 没授权；服务没重启的话 ws 客户端不会用新凭证 |
| 浏览器打不开 `http://unilab-gateway.local` | 1) 出厂时没做 §13.3 mDNS 部署 → 按 §13.3 补做；2) 客户端是老版 Windows → 装 [Bonjour Print Services](https://support.apple.com/kb/DL999)；3) `ssh` 进去 `avahi-resolve -n unilab-gateway.local` 看本机能不能解析，参考 §13.3.4 |
| `avahi-resolve` 超时 / OPi 自己也 ping 不通 `.local` | 多半是 `publish-workstation=no` 没改 / `libnss-mdns` 没装。重跑 §13.3.2 |
| 切换实验室后设备注册失败 `class material resource template not exist` | 云端没有该实验室的设备 schema。**必须在装了完整 ROS2 + unilabos_msgs 的 PC 上**跑 `unilab --upload_registry --addr <env> --ak <NEW_AK> --sk <NEW_SK>`。网关本身不带 ROS2，跑不了这个命令。每个实验室只需做一次 |

### 13.10 自定义 AP SSID / 密码

如果产品需要不同的 AP 名字或密码，改 `wifi_manager.py` 顶部常量：

```python
class WiFiManager:
    AP_SSID_PREFIX = "UniLab-Gateway"   # SSID 前缀
    AP_PASSWORD = "unilab1234"          # AP 密码（≥8 位 WPA2-PSK）
    AP_GATEWAY_IP = "192.168.12.1"      # create_ap 默认就是这个
```

---

## 14. A/B 双分区 + OTA 远程升级

> **目标**：网关产品交付到客户后，**云端 → 网关**远程下发新固件（rootfs 镜像），断电安全、失败自动回滚。本章记录把单分区 Armbian 镜像迁移到 **A/B 双分区** 的全过程、`ota` 命令行工具的设计与用法、以及完整的切换测试。
>
> **本章对应 OTA 开发计划的 D1 阶段（2026-06-17 完成）**：分区改造 + ota shell 工具 + A↔B 双向切换验证。后续 OTA Agent / ThingsBoard 集成在 D2 阶段。

### 14.1 为什么要 A/B 双分区

工业级 IoT 设备远程升级最大的风险是**升到一半断电 / 升完起不来**。A/B 方案把 rootfs 拆成两块同样大小的分区：

```
[ boot ]  [ rootfs-A ]  [ rootfs-B ]  [ data ]
   p1         p2            p3          p4
 共享        当前运行       下次升级目标   持久化
```

升级流程**永远不动当前运行的分区**，只往另一块写：

1. 当前运行 A → OTA Agent 把新镜像 `dd` 到 B（A 完全不动，断电 0 影响）
2. 校验 B 的 sha256 / e2fsck 通过 → 改 `rootdev=UUID=<B>` → reboot
3. **B 起来 + 自检通过** → 切换成功
4. **B 起不来 / 自检失败** → u-boot 回退（或人工 `ota switch A`）→ 系统从 A 启动如初

成本：固件占用空间翻倍（10GB rootfs → 20GB）。换来的是**任意时刻断电都能起来**——这是工业产品的硬指标。

### 14.2 分区方案（32GB TF 卡）

| 分区 | 设备 | 大小 | 文件系统 | LABEL | 内容 |
|---|---|---|---|---|---|
| p1 | `/dev/mmcblk0p1` | 256 MiB | ext4 | `boot` | u-boot 的 `boot.scr` / `orangepiEnv.txt` / kernel / initramfs |
| p2 | `/dev/mmcblk0p2` | 10 GiB | ext4 | `rootfs-A` | A 系统根 |
| p3 | `/dev/mmcblk0p3` | 10 GiB | ext4 | `rootfs-B` | B 系统根 |
| p4 | `/dev/mmcblk0p4` | 剩余（~12 GiB） | ext4 | `data` | 持久化数据：`/data/ota/`（active.txt / 下载缓存 / 日志）、应用产生的数据 |

**关键设计**：

- **boot 分区共享**：A/B 都用同一个内核与 `orangepiEnv.txt`，升级时只换 rootfs，不动内核
- **data 分区共享**：`active.txt`（哪个 rootfs 是 active）、下载缓存、日志放这里，A↔B 切换不丢数据
- **A/B 大小相同**：保证任何一个 rootfs 都能装下完整系统
- **A/B 各有独立 UUID**：避免混淆，挂载/启动用 UUID 而不是 LABEL

`orangepiEnv.txt` 的关键字段：

```
rootdev=UUID=<当前 active 分区的 UUID>
```

u-boot 读这个值生成 kernel cmdline `root=UUID=...`，kernel 据此挂载根分区。**ota switch 就是改这一行**。

### 14.3 出厂迁移：migrate_to_ab.sh

> 脚本路径：`unilabos/gateway/ota/migrate_to_ab.sh`
> 作用：把"已经跑通业务的单分区 Armbian 镜像"一键克隆迁移到一张新 TF 卡上，做成 A/B 双分区结构。**这是出厂前的一次性动作**。

#### 14.3.1 操作流程

操作机：旧卡的 OPi（IP `10.20.31.230`），新卡通过 USB 读卡器接到 OPi 上变成 `/dev/sda`。

```bash
# 0. 备份旧卡 rootfs（防呆）
sudo tar --one-file-system --exclude=/proc --exclude=/sys --exclude=/dev \
    --exclude=/run --exclude=/tmp --exclude=/mnt --exclude=/media \
    -czf /home/orangepi/rootfs-backup.tar.gz /
ls -lh /home/orangepi/rootfs-backup.tar.gz   # ~3 GB

# 1. 把脚本推到 OPi
# (在 dev 机 CMD)
scp D:\Uni-Lab\Uni-Lab-OS\unilabos\gateway\ota\migrate_to_ab.sh orangepi@10.20.31.230:~/

# 2. 跑脚本（指定新卡设备 = /dev/sda）
ssh orangepi@10.20.31.230
sudo bash ~/migrate_to_ab.sh /dev/sda
```

脚本做的事（按顺序）：

| 步骤 | 操作 | 说明 |
|---|---|---|
| 1 | 安全检查 | 拒绝目标 = 当前 root 设备；要求大小 ≥ 16 GiB |
| 2 | `dd if=/dev/mmcblk0 of=/dev/sda bs=1M count=8` | 拷 u-boot bootloader（前 8 MB） |
| 3 | `sfdisk` 写 4 个分区 | 见 §14.2 表格，`*` 启动标在 p1 |
| 4 | `mkfs.ext4 -L <label>` × 4 | 给 4 个分区打文件系统 + LABEL |
| 5 | mount 新分区到 `/mnt/new/{boot,A,B,data}` | 临时挂载 |
| 6 | `cp -a /boot/* /mnt/new/boot/` | 把内核、orangepiEnv.txt 等拷到新 boot 分区 |
| 7 | `rsync -aAHX --numeric-ids /` 到 `/mnt/new/A/`（带排除清单） | 拷整个 rootfs 到 A 分区，**排除 `/proc /sys /dev /run /tmp /mnt /media /boot`** |
| 8 | 改 `/mnt/new/A/etc/fstab` | `/` 用 A 的 UUID、`/boot` 用 boot 的 UUID、`/data` 用 data 的 UUID |
| 9 | 改 `/mnt/new/boot/orangepiEnv.txt` | `rootdev=UUID=<A 的 UUID>` |
| 10 | 创建 `/mnt/new/data/ota/{download,backup,log}/` + `active.txt = "A"` | 初始化 OTA 元数据目录 |
| 11 | `sync` + 卸载所有挂载点 | 完成 |

跑完日志最后会打印 4 个分区的 UUID 与 device path，记下来备用。

#### 14.3.2 启动新卡

1. 关闭 OPi，把新卡从读卡器拔下，插到测试 OPi 的卡槽
2. 上电，等约 60 秒
3. SSH 进去（IP 可能变了，看路由器后台或 mDNS `unilab-gateway.local`）

#### 14.3.3 验证 A 启动正常

```bash
df -h /                    # 期望：/dev/mmcblk0p2 ~10G
findmnt -no UUID /         # 期望：A 的 UUID
cat /proc/cmdline          # 期望：root=UUID=<A 的 UUID>
lsblk -f                   # 看到 4 个分区 + UUID + LABEL
systemctl is-active unilab-gateway   # active
```

如果 `df -h /` 显示 `/dev/mmcblk0p2` + 服务 `active`，A/B 改造完成。**这是出厂状态**。

### 14.4 ota 命令行工具

> 脚本路径（仓库内）：`unilabos/gateway/ota/ota`
> 部署位置：`/usr/local/bin/ota`（`A` 和 `B` 两边都要装）

`ota` 是一个 bash 脚本，封装了 A/B 切换的所有底层操作，给上层（OTA Agent / 运维工程师）一个简洁的 CLI。

#### 14.4.1 子命令一览

| 子命令 | 是否需 sudo | 作用 |
|---|---|---|
| `ota get-active` | 否 | 打印当前 active 分区标签（`A` 或 `B`） |
| `ota get-inactive` | 否 | 打印当前 inactive 分区标签 |
| `ota info` | 否 | 打印 JSON：active/inactive、4 个 UUID、4 个 dev path |
| `ota write <image>` | **必须** | 把镜像 `dd` 到 inactive 分区（带 size 检查） |
| `ota verify <sha256>` | **必须** | 校验 inactive 分区的 sha256 |
| `ota switch [A\|B]` | **必须** | 改 `active.txt` 和 `orangepiEnv.txt`，下次 reboot 生效 |
| `ota rollback` | **必须** | 切换到对面分区（语义等价 `switch <inactive>`） |
| `ota help` | 否 | 帮助 |

设计约定（OTA Agent 编程接口）：

- **读命令**：单行简单文本输出，stderr 干净，方便 grep/awk
- **写/危险命令**：必须 root，否则 exit 1
- **错误**：stderr 红色 + 退出码非 0
- **日志**：所有命令都会写 `/data/ota/log/ota.log`（对 root 自动写，普通用户无写权限时静默跳过）

#### 14.4.2 部署到 OPi

```cmd
:: dev 机 CMD：scp 修复版到 OPi
scp D:\Uni-Lab\Uni-Lab-OS\unilabos\gateway\ota\ota orangepi@10.20.31.55:/tmp/ota
```

```bash
# OPi 上：装到 A 和 B 两边
ssh orangepi@10.20.31.55

# A（当前 active）
sudo install -m 0755 /tmp/ota /usr/local/bin/ota

# B（临时挂上来同步装一份）
sudo mkdir -p /mnt/B
sudo mount /dev/mmcblk0p3 /mnt/B
sudo install -m 0755 /tmp/ota /mnt/B/usr/local/bin/ota
sudo umount /mnt/B && sudo rmdir /mnt/B

rm /tmp/ota

# 验证
file /usr/local/bin/ota
ota get-active
ota info
```

⚠ 必须 **A 和 B 都装**——否则 reboot 进 inactive 分区后那台没有 `ota` 工具，没法切回来。

#### 14.4.3 典型用法（OTA Agent 视角）

```bash
# 1. 看现在 active 是什么
ACTIVE=$(ota get-active)        # → A
INACTIVE=$(ota get-inactive)    # → B

# 2. 把新镜像写到 inactive
sudo ota write /data/ota/download/rootfs-v2.0.img

# 3. 校验
sudo ota verify abc123...sha256

# 4. 切换 active
sudo ota switch     # 不带参数 = 切到 inactive
sudo reboot

# 5. （reboot 后 OTA Agent 自检）失败时回滚
sudo ota rollback
sudo reboot
```

### 14.5 完整切换测试（D1 验收）

> **目标**：在没有真实 OTA 镜像的情况下，用 `dd A → B` 模拟"OTA 把新固件写到 B"，然后跑完 `ota switch B → reboot → 验证 → ota switch A → reboot → 验证`，确认整条链路能工作。

#### 14.5.1 Phase 1：dd 拷贝 A → B（模拟 OTA write）

```bash
# 记下原始元数据（dd 后还原用）
A_UUID=$(lsblk -no UUID /dev/mmcblk0p2)
B_UUID_ORIG=$(lsblk -no UUID /dev/mmcblk0p3)
B_LABEL_ORIG=$(lsblk -no LABEL /dev/mmcblk0p3)

# 确认 B 没挂载
mount | grep mmcblk0p3 && echo "WARN" || echo "OK"

# dd（10 GiB，~16 分钟，速度受 SD 卡限制）
sudo dd if=/dev/mmcblk0p2 of=/dev/mmcblk0p3 bs=4M conv=fsync status=progress
sudo sync
```

dd 后 **B 的 UUID/LABEL 都被覆盖成 A 的**（因为 ext4 superblock 包含 UUID）。

#### 14.5.2 Phase 2：还原 B 的元数据

dd 拷过来的文件系统的 journal 是脏的，必须先 `e2fsck` 才能改 UUID：

```bash
# 修复（自动 yes）
sudo e2fsck -fy /dev/mmcblk0p3

# 还原 UUID 和 LABEL
sudo tune2fs -U "$B_UUID_ORIG" /dev/mmcblk0p3
sudo e2label /dev/mmcblk0p3 "$B_LABEL_ORIG"

sudo blkid /dev/mmcblk0p3
# 期望：LABEL="rootfs-B" UUID="<B 原本的 UUID>"
```

#### 14.5.3 Phase 3：改 B 分区里的 fstab

dd 拷来的 B 里 `/etc/fstab` 的 `/` 行还是 A 的 UUID，B 启动时会找不到 root：

```bash
sudo mkdir -p /mnt/B && sudo mount /dev/mmcblk0p3 /mnt/B

# 把 fstab 里所有 A 的 UUID 替换成 B 的
sudo sed -i "s|$A_UUID|$B_UUID_ORIG|g" /mnt/B/etc/fstab

grep -E '^\s*UUID' /mnt/B/etc/fstab
# 期望：/ 行用 B 的 UUID；/boot 和 /data 行不变（共享）

sudo umount /mnt/B && sudo rmdir /mnt/B
```

#### 14.5.4 Phase 4：ota switch B + reboot

```bash
# 切换（修改 active.txt 和 orangepiEnv.txt）
sudo ota switch B

# 验证
cat /data/ota/active.txt              # B
grep rootdev /boot/orangepiEnv.txt    # rootdev=UUID=<B>
ls -l /boot/orangepiEnv.txt           # -rw-r--r-- ← 重点！见 §14.6 坑 #4

sudo reboot
```

#### 14.5.5 Phase 5：起来后验证从 B 启动

等 30 秒后重新 SSH，跑：

```bash
df -h /                    # 期望：/dev/mmcblk0p3
findmnt -no UUID /         # 期望：B 的 UUID
cat /proc/cmdline          # 期望：root=UUID=<B>
ota get-active             # 期望：B
systemctl is-active unilab-gateway   # active
```

#### 14.5.6 Phase 6：rollback 验证（B → A）

```bash
sudo ota switch A
sudo reboot
# 等 30s 重连，再次验证 df -h / 是 mmcblk0p2 + 服务 active
```

D1 阶段验收标准：**A → B → A 双向切换都能起来，服务都能跑**。✅

### 14.6 故障处理（D1 阶段踩过的 5 个坑）

| # | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | OPi 上 `/usr/local/bin/ota` 报 `cannot execute: required file not found` | dev 机（Cursor / Windows）写出的脚本是 CRLF，shebang 变成 `#!/bin/bash\r`，bash 找 `bash\r` 解释器找不到（错误信息名字误导） | dev 机源文件保存为 LF；OPi 上已装的旧文件用 `sudo sed -i 's/\r$//' /usr/local/bin/ota` 当场修 |
| 2 | `ota get-active` 报 `UUID 不匹配 A/B` 但 `sudo ota info` 显示一致 | `blkid` 普通用户跑要 read 块设备权限，返回空字符串；UUID 比较时 `<某值> != ""` 两次都假，进 else 报错。sudo 没事 | 改用 `lsblk -no UUID`，普通用户从 udev/sysfs 也能读到 UUID |
| 3 | `ota get-active` 后跟 `bash: /data/ota/log/ota.log: Permission denied` | 普通用户没法写 root 创建的日志文件；`echo >> file 2>/dev/null` 屏蔽不掉 bash 在 redirect 阶段（fork 之前）报的错 | 加 `[[ -w "$log_dir" \|\| -w "$LOG_FILE" ]]` 守卫 |
| 4 | `sudo ota switch` 后 `grep` / `awk` 读 `/boot/orangepiEnv.txt` 报 `Permission denied` | 脚本用 `mktemp`（默认 600 权限）+ `mv` 写新内容，**`mv` 保留了源文件的 600 权限**，导致原本 644 的 orangepiEnv.txt 变成 600 | 改用 `install -m 644 -p "$tmp" "$ENV_FILE"; rm -f "$tmp"` |
| 5 | `dd A→B` 后 `tune2fs -U` 报 `requires a freshly checked filesystem. Please run e2fsck -f` | A 在挂载状态下被 dd，文件系统的 journal 是脏的；tune2fs 拒绝改 dirty 的 fs 的 UUID | dd 后先跑 `sudo e2fsck -fy /dev/mmcblk0p3`，再 `tune2fs -U`。**这条以后会写进 ota write 子命令** |

#### 14.6.1 reboot 后 OPi 起不来（应急）

如果 `ota switch B + reboot` 后 5 分钟还连不上：

1. 物理拔电
2. 把 TF 卡插回 dev 机（USB 读卡器），p1 boot 分区会自动挂上来
3. 编辑 `<挂载点>/orangepiEnv.txt`，把 `rootdev=UUID=...` 改回 A 的 UUID
4. 弹出 TF 卡，插回 OPi 上电

> 这是最后的"逃生通道"——只要 boot 分区还能改，就能改回 A 启动。

#### 14.6.2 dev 机上 ota 文件意外又变成 CRLF

Cursor / VSCode 默认在 Windows 上保存为 CRLF。一次性修：

```cmd
python -c "p=r'unilabos/gateway/ota/ota'; d=open(p,'rb').read().replace(b'\r\n',b'\n'); open(p,'wb').write(d); print('done')"
```

长期解决（推荐）：在仓库根加 `.gitattributes`：

```
unilabos/gateway/ota/* text eol=lf
*.sh                   text eol=lf
```

### 14.7 当前状态与后续待办

#### D1 阶段（已完成 2026-06-17）

- [x] **`migrate_to_ab.sh`**：把单分区镜像迁成 A/B 双分区（一次性出厂动作）
- [x] **`ota` 命令行工具**：7 子命令，封装 A/B 切换底层操作
- [x] **A↔B 双向切换验证**：dd A→B + ota switch B + reboot + 服务正常 → ota switch A + reboot + 服务正常
- [x] 5 个 bug 修复 + 文档化（§14.6）

#### D2 阶段（已完成 2026-06-22）→ 详见 §15

- [x] **OTA Agent Python 服务骨架**（`unilabos/gateway/ota/agent.py` 等 6 个模块）
- [x] **systemd unit `unilab-ota-agent.service`** + `/etc/unilab-ota-agent.env` 模板
- [x] **TB MQTT 协议接入**：v1 attributes/telemetry 上行 + v2 chunk-based OTA 下行
- [x] **chunk 下载 + sha256 校验闭环**：在真机上跑通 1.0.1 / 1.0.2 两轮，TB Cloud + Agent 全链路验证 ✓

#### D3 阶段（接下来）

- [ ] **接 ota CLI**：firmware_handler 校验通过后 → `ota write B <file>` → `ota verify B` → `ota switch B` → `sudo reboot`
- [ ] **reboot 后自检 + 自动回滚**：起来 N 分钟内服务不 active 就 `ota rollback + reboot`（用 `/data/ota/pending.flag` 状态机）
- [ ] **fw_state=UPDATED 上报**：reboot 后 Agent 拉到新版本上报"已升级成功"，关掉 TB dashboard 上的 in-progress 状态
- [ ] **u-boot 端 watchdog**：连续 3 次 boot 失败自动切回另一分区（不依赖 userspace）

#### D3+ 阶段

- [ ] **ThingsBoard 服务端**：firmware repo + dashboard + 灰度分组下发
- [ ] **镜像构建流水线**：CI 出 `rootfs-v<X.Y.Z>.img` + sha256
- [ ] **生产环境灰度发布**：先 1 台 → 1 个实验室 → 全量
- [ ] **D2 鲁棒性补丁**（先记下来，不阻塞 D3）：
  - dispatcher 异常退出 → 触发 stop_event → systemd 重启 Agent（目前 dispatcher 退出 Agent 仍活着但聋）
  - 处理 `v2/fw/error` topic（目前丢弃 → 等 chunk_timeout 才 fail）
  - `state.json` 改成每 N chunk 才存（目前每 chunk 都 fsync，SD 卡寿命受影响）
  - `progress_cb` 用 `last_reported` 去重 publish（目前 `percent=0` 时 publish 多次）

涉及的核心文件：

- `unilabos/gateway/ota/migrate_to_ab.sh` — 出厂迁移脚本（一次性）
- `unilabos/gateway/ota/ota` — A/B 切换 CLI（必装到 A 和 B 两边的 `/usr/local/bin/`）
- `/boot/orangepiEnv.txt` — `rootdev=UUID=...` 是 active 的"权威源 1"
- `/data/ota/active.txt` — `A` 或 `B`，是 active 的"权威源 2"（OTA Agent 用这个判断）
- `/data/ota/{download,backup,log}/` — 下载缓存 / 备份 / 日志

---

## 15. OTA Agent：TB MQTT 最小闭环（D2 阶段）

> **D2 目标**：让 OPi 主动连 ThingsBoard，收到云端推送的 firmware 元数据后，按 v2 chunk-based 协议把固件分块下载到本地、做 sha256 校验。**校验通过即停**，不调 `ota` CLI、不 reboot、不切分区——那是 D3 的事。
>
> **为什么先停在校验？** 把"下载链路"和"写盘 + 切分区"两件事解耦，前者可以反复测、不会刷烂 SD；后者在 D1 阶段已经独立 100% 跑通，D3 拼上去就是一行 subprocess。这是工业 OTA 的标准切法。

### 15.1 整体架构

```
                +─────────────────────────+
                │   ThingsBoard EU Cloud  │
                │   eu.thingsboard.cloud  │
                +────────────┬────────────+
                             │ MQTT (1883)
                             │ v1/devices/me/{telemetry,attributes,...}
                             │ v2/fw/{request,response}/<rid>/chunk/<cid>
                             │ v2/fw/error
                             ▼
+──────────────────────────────────────────+
│  Orange Pi Zero 2W (rootfs-A，10.20.31.55) │
│                                          │
│  systemd: unilab-ota-agent.service       │
│   └─ python -m unilabos.gateway.ota.agent│
│       ├─ tb_client.py  ←─ aiomqtt 封装   │
│       ├─ state.py      ←─ JSON 持久化    │
│       ├─ firmware_handler.py             │
│       └─ agent.py      ←─ asyncio 主循环 │
│                                          │
│  /etc/unilab-ota-agent.env  (TB_HOST/TOKEN, 600 root) │
│  /data/ota/agent_state.json (状态机)     │
│  /data/ota/download/<title>-<ver>.bin    │
│                                          │
│  D3 才接上：→ /usr/local/bin/ota write/verify/switch │
+──────────────────────────────────────────+
```

### 15.2 TB v2 chunk-based OTA 协议（D2 用到的部分）

| 方向 | Topic | 用途 |
|---|---|---|
| ↑ Agent → TB | `v1/devices/me/telemetry` | 上报 `fw_state` / `fw_progress` / `current_fw_*` |
| ↑ Agent → TB | `v1/devices/me/attributes` | 上报 client-side attributes（启动时把 current_fw 钉到云端） |
| ↑ Agent → TB | `v1/devices/me/attributes/request/<id>` | 主动拉 shared attributes（启动时防漏推） |
| ↑ Agent → TB | `v2/fw/request/<rid>/chunk/<cid>` | 请求一个 chunk，payload 是 chunk_size 的字符串 |
| ↓ TB → Agent | `v1/devices/me/attributes` | shared attribute 实时推送（含 `fw_title`/`fw_version`/`fw_size`/`fw_checksum`/`fw_checksum_algorithm`） |
| ↓ TB → Agent | `v1/devices/me/attributes/response/<id>` | 拉取响应 |
| ↓ TB → Agent | `v2/fw/response/<rid>/chunk/<cid>` | binary chunk 数据 |
| ↓ TB → Agent | `v2/fw/error` | broker 处理 OTA 请求失败（**踩坑点 2**） |

**认证**：device access token 当 MQTT username，password 空。

**关键约束**：单 chunk 必须 ≤ broker max packet size。**TB Cloud 上限是 65535 (64KB)，推荐 8KB**（见 §15.7 坑 2）。

### 15.3 代码结构（`unilabos/gateway/ota/`）

```
unilabos/gateway/ota/
├── __init__.py                  # 包标记
├── migrate_to_ab.sh             # (D1) 出厂迁移
├── ota                          # (D1) A/B 切换 CLI
├── agent.py                     # (D2) asyncio 主循环 + signal handler
├── tb_client.py                 # (D2) aiomqtt 封装 + v2 chunk-based OTA 协议
├── state.py                     # (D2) /data/ota/agent_state.json 持久化
├── firmware_handler.py          # (D2) chunk 下载拼装 + sha256 校验
├── unilab-ota-agent.service     # (D2) systemd unit
├── unilab-ota-agent.env.example # (D2) /etc/unilab-ota-agent.env 模板
└── README.md                    # (D2) 端到端文档
```

各模块职责（写得糙就改）：

- **`tb_client.TBClient`**：单 reader（dispatcher 协程）从 `aiomqtt.Client.messages` 拉所有订阅消息，按 topic 分发到 `attribute_updates_queue` / `pending_attr_responses<id>` / `pending_fw_chunks<rid,cid>`。对外暴露 `publish_telemetry / publish_client_attributes / request_shared_attributes / attribute_updates() / request_firmware_chunk()`。
- **`state.OtaState`**：dataclass + 原子写 JSON（先写 tmp、`os.replace`）。两块状态：`current_fw`（当前活动版本）+ `ongoing`（正在下载的版本 + 已收 chunks + state machine）。重启可恢复。
- **`firmware_handler.download_firmware`**：循环 `request_firmware_chunk` → 写文件 → `sha256.update()` → 全部完成后校验。**校验通过停在 `STATE_VERIFIED`，不再往下走**。
- **`agent.OtaAgent`**：把上面三个串起来。主循环 = `async with TBClient as tb: 上报 → poll 初始 fw → listen attribute updates → 收到 fw 推送就 spawn _maybe_handle_firmware → _do_update 包了 download_firmware`。

### 15.4 状态机

```
                  +───────────+
   启动恢复  →    │  UPDATED  │  ←── reboot 后从 ota 拉到新版本（D3）
                  +─────┬─────+
                        │ 收到 TB 推送新 firmware
                        ▼
                 +────────────+
                 │ DOWNLOADING│ ──┐
                 +─────┬──────+   │
                       │          │ 任一步骤失败
            chunks 全收 │          │
                       ▼          │
                 +────────────+   │
                 │ DOWNLOADED │ ──┤
                 +─────┬──────+   │
                       │          │
              sha256 通过│          │
                       ▼          ▼
                 +────────────+ +────────+
   D2 停这里 →    │  VERIFIED  │ │ FAILED │
                 +─────┬──────+ +────────+
                       │
                       │ D3 才有：ota write/verify/switch
                       ▼
                  +────────────+
                  │  SWITCHED  │ → reboot → 起来后 → UPDATED
                  +────────────+
```

**持久化文件 `/data/ota/agent_state.json` 示例**（VERIFIED 状态）：

```json
{
  "current_fw": {"title": "uni-lab-gateway", "version": "1.0.0"},
  "ongoing": {
    "rid": 2,
    "title": "uni-lab-gateway",
    "version": "1.0.2",
    "size": 5242880,
    "checksum": "9c357e6b...",
    "checksum_alg": "SHA256",
    "chunk_size": 8192,
    "total_chunks": 640,
    "received_chunks": 640,
    "state": "VERIFIED",
    "download_path": "/data/ota/download/uni-lab-gateway-1.0.2.bin",
    "error": null
  }
}
```

### 15.5 端到端实操记录（2026-06-22 真机验收）

#### 15.5.1 准备 ThingsBoard 实例

不想本地起 Docker，直接用 ThingsBoard EU Cloud（demo 性质，免费）：

1. 注册 [eu.thingsboard.cloud](https://eu.thingsboard.cloud) 账号
2. **Devices → +** 添加 `unilab-ota-test-001`，profile = default
3. 点 device → **Manage credentials** → copy **Access token** 备用

> 注意：`demo.thingsboard.io` 和 `eu.thingsboard.cloud` 是**两个独立实例**，token 不通用（§15.6 坑 1）。

#### 15.5.2 OPi 上部署 Agent

```bash
# 在 dev 机推代码
cd D:\Uni-Lab\Uni-Lab-OS
scp -r unilabos/gateway/ota/*.py unilabos/gateway/ota/__init__.py \
    unilabos/gateway/ota/unilab-ota-agent.* \
    orangepi@10.20.31.55:~/code/Uni-Lab-OS/unilabos/gateway/ota/

# OPi 上装依赖（aiomqtt 已写进 requirements.txt）
ssh orangepi@10.20.31.55
cd ~/code/Uni-Lab-OS
.venv/bin/pip install -U aiomqtt

# 安装 systemd unit + env
sudo install -m 644 unilabos/gateway/ota/unilab-ota-agent.service \
    /etc/systemd/system/unilab-ota-agent.service
sudo install -m 600 -o root -g root unilabos/gateway/ota/unilab-ota-agent.env.example \
    /etc/unilab-ota-agent.env

# 改 /etc/unilab-ota-agent.env，填三件事：
#   TB_HOST=eu.thingsboard.cloud
#   TB_TOKEN=<上面 copy 的 access token>
#   CURRENT_FW_TITLE=uni-lab-gateway
#   CURRENT_FW_VERSION=1.0.0
#   CHUNK_SIZE=8192          ← 重点！见 §15.6 坑 2
sudo nano /etc/unilab-ota-agent.env

sudo mkdir -p /data/ota/download
sudo chown -R orangepi:orangepi /data/ota

sudo systemctl daemon-reload
sudo systemctl enable --now unilab-ota-agent
sudo journalctl -u unilab-ota-agent -f --no-pager
```

期望立刻看到：

```
[INFO] 启动 OTA Agent: TB=eu.thingsboard.cloud:1883 current=uni-lab-gateway/1.0.0 chunk_size=8192
[INFO] TB MQTT 已连接，订阅生效
[INFO] 已上报当前固件: uni-lab-gateway/1.0.0 状态=UPDATED
[INFO] TB 未分配 firmware，等推送
```

#### 15.5.3 dev 机造 fake firmware

```powershell
cd D:\Uni-Lab\Uni-Lab-OS

python -c "import os; open('fake-firmware-v1.0.1.bin','wb').write(os.urandom(5*1024*1024))"
python -c "import hashlib; print(hashlib.sha256(open('fake-firmware-v1.0.1.bin','rb').read()).hexdigest())"
```

记下 sha256，后面校验对照（其实 TB UI 会自动算）。

#### 15.5.4 TB UI 上传 firmware + 分配给 device

1. **Resources → Otaupdates → +**
2. Title: `uni-lab-gateway`（必须和 `CURRENT_FW_TITLE` 同名），Version: `1.0.1`，Type: Firmware，Device profile: default
3. 上传 `fake-firmware-v1.0.1.bin`
4. **Devices → unilab-ota-test-001 → Details → Assigned firmware** → 下拉选 `uni-lab-gateway 1.0.1` → **Save**

#### 15.5.5 Agent 端收到推送并下载

OPi 日志应该实时出现：

```
[INFO] 收到 firmware 推送: {'fw_title': 'uni-lab-gateway', 'fw_version': '1.0.1', 'fw_size': 5242880, 'fw_checksum': '9c357e6b...', 'fw_checksum_algorithm': 'SHA256'}
[INFO] 开始 OTA: rid=1 title=uni-lab-gateway version=1.0.1 size=5242880
[INFO] 开始下载 uni-lab-gateway-1.0.1 (size=5242880, chunks=640, chunk_size=8192) -> /data/ota/download/uni-lab-gateway-1.0.1.bin
[INFO] 下载进度: 10/640 chunks (1.6%)
[INFO] 下载进度: 20/640 chunks (3.1%)
... (~7 分钟，~13 KB/s)
[INFO] 下载进度: 640/640 chunks (100.0%)
[INFO] sha256 校验: 算出 9c357e6b... 期望 9c357e6b...
[INFO] 校验通过 ✓ - D2 阶段到此结束（不调 ota CLI），文件留在: /data/ota/download/uni-lab-gateway-1.0.1.bin
[INFO] ✓ D2 完成: uni-lab-gateway/1.0.1 已下载并校验
```

#### 15.5.6 OPi 端 5 步验证清单

```bash
# 1. 文件存在 + 大小正确（5.0M）
ls -lh /data/ota/download/

# 2. sha256 和 TB 一致
sha256sum /data/ota/download/uni-lab-gateway-1.0.1.bin

# 3. state.json 标记 VERIFIED
cat /data/ota/agent_state.json | python3 -m json.tool
# 看 ongoing.state == "VERIFIED" && received_chunks == total_chunks

# 4. Agent 还在跑
systemctl status unilab-ota-agent --no-pager

# 5. 日志没异常
sudo journalctl -u unilab-ota-agent --since "16:00" --no-pager | grep -E "ERROR|WARNING|✓"
```

#### 15.5.7 第二轮（验证实时推送 path）

把 `1.0.1` 换成 `1.0.2` 重跑 §15.5.3~15.5.5。**Agent 不用重启**，attribute_updates() 协程会实时收到新推送。

第二轮日志关键证据：
- `rid=2`（不是 1）→ 是新会话
- `fw_version: 1.0.2`
- 两份 sha256（dev 算的 / Agent 算的）完全一致

### 15.6 故障处理（D2 阶段踩过的 2 个坑）

| # | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | `aiomqtt.exceptions.MqttConnectError: [code:135] Not authorized` | TB 配的是 `demo.thingsboard.io` 但 token 是从 `eu.thingsboard.cloud` 取的。两个实例数据不通，token 不互认 | 改 `/etc/unilab-ota-agent.env` 的 `TB_HOST=eu.thingsboard.cloud`，重启 Agent |
| 2 | 启动正常、收到 firmware 推送、开始下载——然后 dispatcher 退出 `MqttCodeError [code:128] Unspecified error` + `v2/fw/error`，10 秒后 chunk timeout | `CHUNK_SIZE=524288` (512KB) 超过 TB Cloud broker max packet size (65535)。broker 处理 chunk 请求时回 `v2/fw/error` topic，然后主动 disconnect 客户端 | 改 `CHUNK_SIZE=8192` 重启 Agent。8KB 是 TB 官方推荐值，稳 |

**两个坑的本质都是"配置 vs 服务端约束不匹配"**——本地代码无 bug，但运行环境的实例 / 限制和我们的默认值不一致。这种坑只能靠实机调试发现。

### 15.7 当前状态与 D3 待办

#### D2 阶段（已完成）

- [x] **6 个 Python 模块**：`agent.py` / `tb_client.py` / `state.py` / `firmware_handler.py` / `__init__.py` + README
- [x] **systemd unit + env 模板**：root 启动、orangepi 用户跑、`/etc/unilab-ota-agent.env` 是 600 权限
- [x] **TB MQTT 协议接入**：连接 / shared attribute pull / push 监听 / chunk 请求 / telemetry 上报
- [x] **chunk 下载 + sha256 校验**：1.0.1 / 1.0.2 两轮完整跑通
- [x] **状态机 + 持久化**：`/data/ota/agent_state.json` 原子写、重启可恢复
- [x] **依赖 `aiomqtt>=2.0`** 加进 `requirements.txt`

#### D3 阶段（接下来）

详见 §14.7 末尾的 D3 待办清单。核心就一句话：**在 firmware_handler 校验通过后，串起 D1 的 `ota` CLI（write → verify → switch）→ `sudo reboot`，并加 reboot 后的自检 + 自动回滚**。

#### 涉及的 OPi 上文件位置

- `/etc/unilab-ota-agent.env` — TB 配置（root 600，含 device token，**不要 commit**）
- `/etc/systemd/system/unilab-ota-agent.service` — systemd 启动配置
- `/data/ota/agent_state.json` — 状态机 + 断点信息（OTA Agent 自动写）
- `/data/ota/download/<title>-<ver>.bin` — 下载缓存
- `~/code/Uni-Lab-OS/unilabos/gateway/ota/` — 代码（dev 机 scp 推送）

---

## 16. 量产母机克隆指南（出厂前必读）

> **场景**：用一台调试好的 OPi 作为"母机"，dd 镜像 → 烧到其它 SD 卡 → 装到批量新机器上。
>
> **核心问题**：哪些数据是"个性化的"，**克隆出去必须每台不同**？哪些可以直接共享？
>
> 本节按"谁负责"分成 4 类清单，量产前**必须**对照走一遍。

### 16.1 类别 A：硬件天然唯一（什么都不用做）

| 项 | 来源 | 量产时 |
|---|---|---|
| 设备树 serial-number | SoC 内部固化（如 H618） | 一开机就不同 |
| WiFi MAC | WiFi 模块 EEPROM | 一开机就不同 |
| **AP 配网 SSID** | `wifi_manager.py:_generate_ap_ssid()` 用 MAC 后 4 位**自动拼**（如 `UniLab-Gateway-5121`） | **每台自动不同，不用动手** ✓ |
| AP 配网默认密码 | 写死 `unilab1234`（`wifi_manager.py:AP_PASSWORD`） | 每台都一样，**可以接受**（首次配网用，AP 模式不联网） |

### 16.2 类别 B：克隆前**必须清理**（不清就出事）

| 项 | 风险 | 处理 |
|---|---|---|
| `/etc/ssh/ssh_host_*_key*`（4 对：rsa/ecdsa/ed25519/dsa） | **严重安全问题**：所有克隆机共享同一组 SSH 私钥，任何一台被攻破等于全暴露 | 克隆前删除，首次开机 sshd 自动重生成 |
| `/etc/machine-id` + `/var/lib/dbus/machine-id` | journald / TB / 各种依赖 machine-id 的服务会混乱 | 克隆前 `truncate -s 0 /etc/machine-id`，systemd 首次启动自动重生成 |
| `/etc/hostname` + `/etc/hosts`（如母机已改成 `unilab-gateway-XXXX`） | 克隆出去全是同一个 XXXX，mDNS 同名冲突 | 克隆前回写 placeholder，首次开机用设备树 serial 自动生成新 |
| `/etc/NetworkManager/system-connections/*.nmconnection` | 母机调试时连过的 WiFi（含明文密码）会泄漏给客户 | 克隆前删除 |
| `~/.ssh/authorized_keys` 和 `~/.bash_history` | 你的公钥 / 调试历史泄漏 | 克隆前清空（注意：删了 authorized_keys 之后再 ssh 不进来；如还要测就最后再删） |

### 16.3 类别 C：客户首次配网时**填**（出厂留空白）

| 文件 | 字段 | 由谁填 |
|---|---|---|
| `/etc/unilab-gateway.env` | `UNILABOS_BASICCONFIG_AK` / `SK` / `MOUNT_UUID` / `WS_URL` / `HTTP_URL` | 客户用平台前端按钮一键打过来（§13.7） |
| `/etc/unilab-ota-agent.env` | `TB_TOKEN`（**每台 OPi 在 TB 上注册成独立 device，token 不同**） | 出厂 SOP：工厂网络下注册取 token，自动写入 |
| `/etc/unilab-ota-agent.env` | `CURRENT_FW_VERSION` | 出厂烧的固件版本号（如 `1.0.0`） |

⚠️ **`/etc/unilab-ota-agent.env` 包含 TB device token，绝对不能跨机共享**。母机的 token 克隆出去 = 一堆机器同时用同一个云端身份，必冲突。

### 16.4 类别 D：OTA 历史数据**必须清**

| 路径 | 处理 |
|---|---|
| `/data/ota/agent_state.json` | **删** |
| `/data/ota/download/*.bin` | **删** |
| `/data/ota/log/*` | **删** |
| `/data/ota/active.txt` | **留**（这是 AB 分区状态的权威源 2，要保留母机最后的 active 字符） |

### 16.5 类别 E：产品 / 出厂 SOP 层面（不在 OPi 内部）

| 项 | 谁管 |
|---|---|
| 平台前端给每台网关起友好名（`gw-001` / `gw-002` / `实验柜1`） | 客户在平台配置，跟 OPi 本身无关 |
| 外壳标签：短码（如 `33ed`） + AP SSID + AP 密码 + 二维码（含 mDNS URL） | 出厂 SOP |
| TB 上注册 device 并取 token 写入 `/etc/unilab-ota-agent.env` | 出厂 SOP（脚本化） |

### 16.6 出厂流水线（5 步）

```
STEP 0: 准备母机（这台 33ed 就是）
  └─ 装好系统、Python、依赖、ota CLI、OTA Agent、跑完整测试

STEP 1: 在母机上跑 factory-seal.sh（待开发）
  ├─ 清理类别 B 全部
  ├─ 重置类别 C 的 env 为空模板
  ├─ 清理类别 D 的 OTA 历史
  ├─ enable unilab-first-boot.service（待开发）
  └─ poweroff

STEP 2: dd 母机 SD 卡 → factory-image-v<X.Y.Z>.img.gz
  └─ 算 sha256 + 上传到工厂镜像仓库

STEP 3: 量产烧录 → 装到新 OPi

STEP 4: 新机首次开机
  ├─ unilab-first-boot.service 触发（待开发）
  │   ├─ 用设备树 serial 后 4 位设 hostname = unilab-gateway-<XXXX>
  │   ├─ sshd 自动重生 host keys
  │   ├─ systemd 自动重生 machine-id
  │   ├─ avahi 重启，用新 hostname 广播
  │   └─ 自我 disable
  └─ /etc/unilab-gateway.env 空 → 进 AP 配网模式等待

STEP 5: 工厂网络下注册 TB device 取 token（脚本化）
  ├─ 写 /etc/unilab-ota-agent.env
  ├─ 重启 unilab-ota-agent，等 TB 上线 1 次确认通讯 OK
  └─ 打印外壳标签（短码 + AP 信息）→ 入库发货
```

### 16.7 待开发的两个脚本 + 一个 systemd unit（D3 之后做）

- **`unilabos/gateway/factory/factory-seal.sh`**：在母机上跑、做封板前清理。脚本骨架：

```bash
#!/bin/bash
set -euo pipefail
sudo rm -f /etc/ssh/ssh_host_*
sudo truncate -s 0 /etc/machine-id
sudo rm -f /var/lib/dbus/machine-id
sudo rm -f /etc/NetworkManager/system-connections/*.nmconnection
sudo install -m 600 unilab-gateway.env.example /etc/unilab-gateway.env
sudo rm -f /etc/unilab-ota-agent.env
sudo rm -rf /data/ota/download/* /data/ota/log/* /data/ota/agent_state.json
sudo hostnamectl set-hostname unilab-gateway-PLACEHOLDER
sudo sed -i 's/\bunilab-gateway-[0-9a-fA-F]\{4\}\b/unilab-gateway-PLACEHOLDER/g' /etc/hosts
sudo systemctl enable unilab-first-boot.service
sudo systemctl poweroff
```

- **`unilabos/gateway/factory/first-boot.sh`**（装到 `/usr/local/sbin/`）：新机首次开机自动跑，跑完自禁用。脚本骨架：

```bash
#!/bin/bash
set -euo pipefail
FLAG=/var/lib/unilab/first-boot.done
[ -f "$FLAG" ] && exit 0
SERIAL=$(cat /sys/firmware/devicetree/base/serial-number | tr -d '\0')
SHORT="${SERIAL: -4}"
NEW_NAME="unilab-gateway-${SHORT}"
hostnamectl set-hostname "$NEW_NAME"
sed -i "s/\bunilab-gateway-PLACEHOLDER\b/$NEW_NAME/g" /etc/hosts
ssh-keygen -A  # 重生 SSH host keys
systemd-machine-id-setup
systemctl restart avahi-daemon
mkdir -p /var/lib/unilab && touch "$FLAG"
systemctl disable unilab-first-boot.service
echo "首次初始化完成: hostname=$NEW_NAME"
```

- **`unilabos/gateway/factory/unilab-first-boot.service`**（装到 `/etc/systemd/system/`）：

```ini
[Unit]
Description=Uni-Lab Gateway First Boot Initialization
ConditionPathExists=!/var/lib/unilab/first-boot.done
Before=avahi-daemon.service unilab-gateway.service unilab-ota-agent.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/first-boot.sh
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
```

### 16.8 取设备短码的标准代码片段（hostname / 二维码 / TB device name 等多处共用）

```bash
get_gateway_short_code() {
    # 优先 1: 设备树 SoC serial（OPi Zero 2W / H618 焊死，OTA 重装不变）
    local serial=$(cat /sys/firmware/devicetree/base/serial-number 2>/dev/null | tr -d '\0')
    # 优先 2: /proc/cpuinfo Serial（其它 ARM 板可能用这个）
    [ -z "$serial" ] && serial=$(awk '/^Serial/ {print $NF}' /proc/cpuinfo)
    # Fallback: WiFi MAC（去掉冒号）
    if [ -z "$serial" ] || [ "$serial" = "0000000000000000" ]; then
        serial=$(cat /sys/class/net/wlan0/address 2>/dev/null | tr -d ':')
    fi
    echo "${serial: -4}"
}
```

### 16.9 当前母机（33ed）的状态

| 项 | 值 / 状态 |
|---|---|
| 设备树 serial-number | `33802000140f33ed` |
| WiFi MAC | `f4:1a:c1:66:51:21` |
| 短码 | **`33ed`** |
| hostname（已改） | `unilab-gateway-33ed` |
| mDNS 域名 | `http://unilab-gateway-33ed.local` |
| AP SSID（自动生成） | `UniLab-Gateway-5121`（MAC 后 4 位） |
| 角色 | 当前是**调试机**，后续要当母机量产前需走 §16.2 - §16.4 清理 |

---

## 附录 A：完整 gateway.yaml 示例

```yaml
# Uni-Lab IoT 网关配置示例
# 顶层：网关本身的元信息

machine_name: gw-zero2w-001            # 网关名（host_node 注册到云端的 name）

# 设备清单（每个设备一项）
devices:
  # ============================================================
  # 兰格 SY-03B 注射泵（USB-RS485）
  # ============================================================
  - device_id: pump-sy03b-001
    name: SY-03B 注射泵
    driver: unilabos.devices.pump_and_valve.runze_backbone.RunzeSyringePump
    registry_class: syringe_pump_with_valve.runze.SY03B-T06

    # 驱动 __init__ 的关键字参数
    init:
      port: /dev/ttyUSB0
      address: "1"
      max_volume: 25.0

    # 周期性轮询的属性（驱动里有 get_<prop>() 方法或 @property 字段）
    properties:
      - status
      - position
      - valve_position

    poll_interval: 1.5                 # 轮询间隔（秒）
    report_unchanged: false            # false: 值变化才上报；true: 每次都上报
    property_timeout: 5.0              # 单次属性读超时
    action_timeout: 120.0              # 单次动作执行超时

  # ============================================================
  # 第二台设备示例（注释，按需启用）
  # ============================================================
  # - device_id: stirrer-001
  #   name: 大龙磁力搅拌器
  #   driver: unilabos.devices.heaterstirrer.dalong.DalongStirrer
  #   registry_class: stirrer.dalong.MS-H280-Pro
  #   init:
  #     port: /dev/ttyUSB1
  #     address: "1"
  #   properties:
  #     - speed
  #     - temperature
  #   poll_interval: 2.0
  #   property_timeout: 5.0
  #   action_timeout: 30.0

  # ============================================================
  # Mock 设备（测试用，不需要真硬件）
  # ============================================================
  # - device_id: mock-001
  #   name: Mock 测试设备
  #   driver: MOCK
  #   properties:
  #     - temperature
  #     - humidity
  #   poll_interval: 1.0
```

---

## 附录 B：网关代码结构

```
unilabos/gateway/
├── __init__.py
├── main.py                       # 入口：CLI、加载 yaml、配网检查、启动 worker、连 WS
├── ws.py                         # 极简 WebSocket 客户端（连接/重连/认证/收发）
├── device.py                     # DeviceWorker：包装任意 Python 驱动
│                                 # - 周期 telemetry（带超时保护）
│                                 # - 接 job_start 派发到驱动方法
│                                 # - USB 热拔插 / 设备断电后自动重建 driver
├── register.py                   # 启动时 HTTP POST /edge/material 注册资源树
│                                 # - 先注册 host_node
│                                 # - 再以 host_node.cloud_uuid 为 parent 注册设备
├── discovery/                    # USB 自动发现（指纹库）
│   ├── scanner.py                # 启动时主动扫描所有串口
│   ├── probe.py                  # 单端口指纹探测
│   ├── hotplug_handler.py        # udev 触发的入口（写信号文件）
│   └── 99-unilab-gateway.rules   # udev 规则（要 cp 到 /etc/udev/rules.d/）
└── provisioning/                 # WiFi 配网向导
    ├── wifi_manager.py           # nmcli + create_ap 包装
    ├── web_server.py             # FastAPI 配网网页后端
    ├── templates/provision.html  # 网页前端
    └── sudoers.d.unilab-gateway  # 免密 sudo 规则（要 install 到 /etc/sudoers.d/）
```

关键设计决策：

- **完全独立于 ROS 2**：不依赖 `rclpy` 等任何 ROS 2 包，可在最简 Python 环境运行
- **所有驱动调用走专用单线程 ThreadPoolExecutor**：避免 stuck 的 read 操作占满全局线程池
- **超时不靠 `asyncio.wait_for`**（对 `run_in_executor` 不真起作用），改用 `asyncio.wait + 超时` + `executor.shutdown(wait=False)` 抛弃 stuck 线程
- **驱动重建用临时 ThreadPoolExecutor**：pyserial `Serial.__init__` 偶尔会在内核 IO 层 stuck 几分钟，临时池 + `shutdown(wait=False)` 才能真正实现 10 秒超时

---

## 附录 C：文件位置速查表

### 香橙派上

| 路径 | 内容 | 修改后操作 |
|---|---|---|
| `~/gateway.yaml` | 设备清单 | `sudo systemctl restart unilab-gateway` |
| `/etc/unilab-gateway.env` | ak/sk/url/mount_uuid（root 可读） | **首选：浏览器打开管理后台 §13.7**；备选：`sudo nano` 后 `sudo systemctl restart unilab-gateway` |
| `/etc/systemd/system/unilab-gateway.service` | systemd 服务定义 | `sudo systemctl daemon-reload && sudo systemctl restart unilab-gateway` |
| `/etc/sudoers.d/unilab-gateway` | 配网/重启相关免密 sudo 规则 | `sudo visudo -cf /etc/sudoers.d/unilab-gateway` 校验 |
| `/etc/avahi/avahi-daemon.conf` | mDNS 守护进程主配置（`publish-workstation=yes`） | `sudo systemctl restart avahi-daemon` |
| `/etc/avahi/services/unilab-gateway-admin.service` | mDNS 服务广播（`_http._tcp` 80 端口） | `sudo systemctl restart avahi-daemon` |
| `/etc/hostname` & `/etc/hosts` | 主机名 `unilab-gateway` | `sudo hostnamectl set-hostname xxx` + 改 hosts |
| `/etc/udev/rules.d/99-unilab-gateway.rules` | USB 热插拔触发指纹识别 | `sudo udevadm control --reload-rules` |
| `/tmp/unilab-gateway/hotplug/` | 热插拔信号文件（运行时生成） | 主进程自动清理 |
| `~/code/Uni-Lab-OS/` | 仓库代码（venv 也在这） | `sudo systemctl restart unilab-gateway` |
| `~/code/Uni-Lab-OS/.venv/` | Python 虚拟环境 | 装新包后重启服务 |
| `/dev/ttyUSB0` | USB 串口设备节点 | - |
| `journalctl -u unilab-gateway` | 日志 | - |
| `/usr/local/bin/ota` | A/B 切换 CLI（**A 和 B 两边都装**） | `sudo install -m 0755 ...` |
| `/boot/orangepiEnv.txt` | u-boot 启动配置：`rootdev=UUID=...` 决定下次 boot 哪个分区 | `sudo ota switch <A\|B>` 改、改完 `sudo reboot` 生效 |
| `/data/ota/active.txt` | OTA 元数据：A 或 B（active 的"权威源 2"） | `sudo ota switch` 自动改 |
| `/data/ota/{download,backup,log}/` | OTA 工作目录：下载缓存 / 备份 / 日志 | OTA Agent 自动管理 |
| `/etc/systemd/system/unilab-ota-agent.service` | OTA Agent systemd 服务定义 | `sudo systemctl daemon-reload && sudo systemctl restart unilab-ota-agent` |
| `/etc/unilab-ota-agent.env` | TB host/token/CURRENT_FW_VERSION/CHUNK_SIZE（root 600，**含 device token 别 commit**） | `sudo nano` 后 `sudo systemctl restart unilab-ota-agent` |
| `/data/ota/agent_state.json` | OTA Agent 状态机持久化（current_fw + ongoing） | Agent 自动写，**不要手改** |
| `/dev/mmcblk0p1` | boot 分区（A/B 共享） | - |
| `/dev/mmcblk0p2` | rootfs-A | - |
| `/dev/mmcblk0p3` | rootfs-B | - |
| `/dev/mmcblk0p4` | data 分区 | - |

### Windows 上

| 路径 | 内容 |
|---|---|
| `D:\Uni-Lab\Uni-Lab-OS\unilabos\gateway\` | 网关代码（编辑后用 scp 推到香橙派） |
| `D:\Uni-Lab\Uni-Lab-OS\unilabos\gateway\ota\migrate_to_ab.sh` | 一次性出厂迁移脚本（单分区 → A/B） |
| `D:\Uni-Lab\Uni-Lab-OS\unilabos\gateway\ota\ota` | A/B 切换 CLI 源文件（**保存为 LF 行尾**，scp 后装到 OPi） |
| `C:\Users\dp1\.ssh\id_ed25519` | SSH 私钥 |
| `C:\Users\dp1\.ssh\id_ed25519.pub` | SSH 公钥 |
| `C:\Users\dp1\.ssh\known_hosts` | 已信任的主机列表 |

### 常用 SSH/SCP 命令模板

```powershell
# 进香橙派
ssh orangepi@10.20.31.230

# 推单个文件
scp <本地文件> orangepi@10.20.31.230:<远程路径>

# 推整个目录
scp -r <本地目录> orangepi@10.20.31.230:<远程路径>

# 拉日志到本地分析
scp orangepi@10.20.31.230:/tmp/some.log .
```

---

## 后续路线（参考 PDF 文档 v0.4）

当前完成度：

- [x] 网关运行时（Python，无 ROS 2）
- [x] WebSocket 客户端（认证/重连/消息分发）
- [x] HTTP 资源树注册
- [x] 设备 telemetry + action 执行
- [x] 驱动级自动重连（USB 热拔插 / 设备断电恢复）
- [x] systemd 开机自启 + 自动重启
- [x] **USB 热插拔事件检测**：udev 触发指纹识别，新设备插上自动注册
- [x] **指纹库（部分）**：注射泵、电导传感器等已支持
- [x] **WiFi AP + Captive Portal 配网**：无显示器开箱即可配网

待办（按优先级）：

- [ ] **指纹库扩充**：被动监听 → SCPI → Modbus → 通用 Raw 透传
- [ ] **本地缓存 + 断线重传**：网络断开时数据不丢，恢复后通过 `telemetry_batch` 批量补传
- [ ] **更多设备驱动**：蠕动泵、磁力搅拌器、天平、pH 计、管式炉
- [ ] **LED 状态指示灯**（需要硬件配合）
- [ ] **ThingsBoard 集成**：用 TB CE 做时序存储/告警/OTA，省 ~4 周自建
- [x] **OTA 升级 D1 阶段**：A/B 分区改造 + `ota` 切换工具 + A↔B 双向切换验证（见 §14）
- [x] **OTA 升级 D2 阶段**：OTA Agent Python 服务 + TB MQTT 接入 + chunk 下载 + sha256 校验闭环（见 §15）
- [ ] **OTA 升级 D3 阶段**：firmware_handler 接 `ota` CLI（write/verify/switch）+ reboot 自检 + 自动回滚
- [ ] **OTA 升级 D3+ 阶段**：TB 服务端 firmware repo / dashboard / 灰度分组、镜像构建流水线
- [ ] **量产母机克隆指南落地**（见 §16）：`factory-seal.sh` + `first-boot.sh` + `unilab-first-boot.service` 三个文件
- [ ] **多网关方案** A 落地：平台前端按钮加"输入短码"提示框，URL 拼 `http://unilab-gateway-<short>.local/...`

---

**文档版本**：v1.6  
**最后更新**：2026-06-23  
**维护**：随网关代码变更同步更新

**v1.1 变更**：新增 §12 USB 热插拔自动识别、§13 WiFi 配网向导，对应配网代码 (`unilabos/gateway/provisioning/`)、指纹库代码 (`unilabos/gateway/discovery/`) 以及附录 C 的 sudoers / udev 配置文件位置。

**v1.2 变更**：网关 Web 服务器扩展为 `mode=provisioning` / `mode=management` 二合一：

- AP 配网模式不变（§13.6）
- STA 模式下**常驻**管理后台（§13.7），用户可在同 WiFi 浏览器修改 AK/SK / 重置 WiFi，**省掉 ssh + sudo nano /etc/unilab-gateway.env**
- `sudoers.d.unilab-gateway` 增加 `tee` / `cat /etc/unilab-gateway.env` / `systemctl restart unilab-gateway` 三组免密授权（§13.2）
- 老版本部署升级路径：重跑 §13.2 的 `sudo install ...` 即可

**v1.3 变更**：新增 §13.3 mDNS 部署，让网关有**固定网址** `http://unilab-gateway.local`：

- 用户**不再需要查 IP**，浏览器永远用同一个网址打开管理后台
- 换 WiFi、换 IP、新客户端 —— 全自动跟随，**零运维**
- 客户端兼容：macOS / iOS / Android / Linux / Win10 1809+ 原生支持；老版 Windows 装 [Bonjour Print Services](https://support.apple.com/kb/DL999) 一次即可
- 老版本部署升级路径：跑 §13.3.1 + §13.3.2 的命令一次即可；附录 C 增加 `/etc/avahi/*` 文件位置说明
- 故障处理表 §13.9 新增 mDNS 排查项

**v1.4 变更**：新增 §14 A/B 双分区 + OTA 远程升级（D1 阶段成果，2026-06-17）：

- **§14.1-14.2 设计**：A/B 双分区（p2/p3 各 10GiB） + 共享 boot（p1）+ 共享 data（p4）的方案与原理
- **§14.3 一次性出厂迁移**：`migrate_to_ab.sh` 把单分区 Armbian 镜像克隆到新 32GB TF 卡并切成 A/B
- **§14.4 ota 命令行工具**：`/usr/local/bin/ota`（7 子命令：get-active / get-inactive / info / write / verify / switch / rollback），是 OTA Agent 的底层接口
- **§14.5 完整切换测试**：dd A→B 模拟 OTA write + e2fsck + tune2fs UUID + 改 fstab + ota switch B + reboot + ota switch A + reboot，**A↔B 双向闭环全部跑通** ✅
- **§14.6 5 个踩过的坑**：CRLF shebang / blkid 权限 / log 写权限 / mv 保留 mktemp 600 / dd 后必须 e2fsck —— 都已修复
- **§14.7 后续待办**：D2 OTA Agent Python 服务（订阅 TB / 调 ota write→verify→switch / reboot 自检 + 自动回滚）+ D3 TB 服务端 + 镜像构建流水线

**v1.5 变更**：新增 §15 OTA Agent：TB MQTT 最小闭环（D2 阶段成果，2026-06-22）：

- **§15.1-15.2 设计**：D2 范围（只到 sha256 校验，不调 ota CLI），整体架构图，TB v2 chunk-based OTA 协议 topic 表
- **§15.3-15.4 代码**：`unilabos/gateway/ota/` 新增 6 个 Python 模块（agent / tb_client / state / firmware_handler / __init__ / README） + systemd unit + env 模板；状态机 5 态（UPDATED → DOWNLOADING → DOWNLOADED → VERIFIED / FAILED）+ `/data/ota/agent_state.json` 原子写持久化
- **§15.5 端到端实操记录**：注册 device → 配 env → dev 机造 fake 5MB 固件 → TB UI 上传 + assign → Agent 自动下载 + 校验，**1.0.1 + 1.0.2 两轮全部跑通** ✅；含 5 步 OPi 端验证清单
- **§15.6 2 个踩过的坑**：(1) TB_HOST 配 `demo.thingsboard.io` 但 token 是 `eu.thingsboard.cloud` 的导致 MQTT 135 Not authorized；(2) `CHUNK_SIZE=524288` 超过 TB Cloud broker max packet size (65535)，broker 主动 disconnect（code 128） → 改成 8192 解决
- **§15.7 D3 待办**：firmware_handler 接 D1 的 `ota` CLI、reboot 后自检 + 自动回滚、u-boot 端 watchdog
- **附录 C** 更新：增加 OTA Agent 相关三个文件位置（`unilab-ota-agent.service` / `/etc/unilab-ota-agent.env` / `/data/ota/agent_state.json`）

**v1.6 变更**：新增 §16 量产母机克隆指南（多网关方案 + 出厂流程基础，2026-06-23）：

- **§16.1-16.4 4 类清单**：硬件天然唯一（AP SSID 已自动用 MAC 后 4 位生成，**无需改造**）/ 克隆前必须清理（SSH host keys / machine-id / hostname / NM 配置 / OTA 历史）/ 客户首次配网填（AK/SK/mount_uuid/TB token）/ 出厂 SOP 层面（外壳标签、TB 注册）
- **§16.5-16.6 出厂流水线 5 步流程**：母机调试 → factory-seal 封板 → dd 镜像 → 量产烧录 → 首次开机自动初始化 + 工厂注册 TB device
- **§16.7 待开发的 3 个文件**：`factory-seal.sh` 母机封板脚本骨架、`first-boot.sh` 新机首次开机自动设 hostname/SSH/machine-id 骨架、`unilab-first-boot.service` systemd oneshot 单元（D3 之后做）
- **§16.8 取短码标准函数**：优先级 SoC 设备树 serial → /proc/cpuinfo Serial → WiFi MAC，hostname/二维码/TB device name 多处复用
- **§16.9 当前母机状态**：本机短码 `33ed`，hostname 已改为 `unilab-gateway-33ed`，mDNS 域名 `http://unilab-gateway-33ed.local`
- **首次实操**：本次确认 OPi Zero 2W 的 SoC serial 来自设备树 `/sys/firmware/devicetree/base/serial-number`（H618 H/W 焊死），后 4 位 hex 作为短码稳定可靠，OTA 升级/换 SD 卡都不变
