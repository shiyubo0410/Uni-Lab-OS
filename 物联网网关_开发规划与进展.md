# Uni-Lab-OS 物联网网关 — 开发规划与进展

> 版本：v1.0　｜　更新日期：2026-07-09　｜　状态：规划中
> 基础文档：《Uni-Lab-OS 设备即节点——架构设计与分阶段实施计划 v0.4》、《物联网 MVP + 运营配合模式讨论纪要 2026-06-11》
> 适用范围：GatewayPro（香橙派 Zero 2W）物联网网关的软硬件开发与量产

---

## 一、背景与目的

### 1.1 为什么做这件事

Uni-Lab-OS 本身已经能把一整套实验室设备编排成自动化工作流，但要跑起来有个隐性门槛：设备得先"接进系统"。对拥有完整边缘服务器、懂 ROS2、愿意配置的团队来说这不是问题；可对绝大多数课题组而言，实验室里更多是零散的中小仪器——天平、pH 计、管式炉、蠕动泵、搅拌器、电化学工作站、单色仪等等。这些设备今天的常态是：

- 数据留在仪器自带软件里，靠人手动导出、U 盘拷、微信发文件；
- 谁在管仪器，谁就得天天做"数据搬运工"，还免不了漏记错记；
- 人不在实验室就完全不知道设备跑到哪一步、有没有出状况；
- 哪怕上层自动化能力再强，只要数据入口还是手工的，整条链路就断在这里。

换句话说，Uni-Lab-OS 缺的是一个**足够轻、足够便宜、不需要专业运维就能上手**的"最后一公里"接入设备。物联网网关就是为了填这个缺口——把接入的技术复杂度全部收到盒子内部，让用户只面对"插线通电"。

### 1.2 我们想交付什么

核心体验可以浓缩成四个字：**插上，看数**。

理想中用户要做的只有：

1. 转接线两头分别插网关和仪器；
2. 网关通电；
3. 等十几秒到半分钟；
4. 手机或网页上这台仪器自己冒出来，数据开始往上走。

整个过程用户**不碰**任何代码、协议、波特率、YAML 或 ROS2 安装；唯一需要人做的，是提前在 leap-lab 前端建好实验室并和网关绑定一次。做到这一步，网关才算真正"开箱即用"，而不是又一个需要工程师调试的采集盒。

### 1.3 放在市场上是什么位置

目标价位落在 ¥500–899，是一款**面向科研实验室的中小设备联网网关**。往上，研华 ADAM 偏工业、偏贵，LabJack（约 $305 起）、NI CompactDAQ（约 $1300 起）要么定位数据采集要么定位高端测控；往下则几乎没有专门为"科研仪器随手联网"设计的品类。我们正好卡在这个空档里，差异点在于：

- **开源开放**：GPL-3.0 + WebSocket/JSON 协议，不锁生态；
- **驱动能长**：驱动是 Python，谁都能加、社区能一起补；
- **不止看得见，还能动**：直接嵌进 Uni-Lab-OS 工作流，采集之外还能自动执行；
- **真·即插即用**：靠协议自适应识别，而非让用户装一堆配置软件；
- **数据可留在校内**：支持仅内网部署，数据不出实验室。

### 1.4 产品线规划

| SKU | 硬件平台 | 定位 | 设备数 | 售价 | 首批 |
|---|---|---|---|---|---|
| Gateway Lite | ESP32-S3 | 已知设备透传 / 指纹匹配 | 1 台 | ¥399–499 | Phase2 起 |
| **Gateway Pro** | **香橙派 Zero 2W** | **完整 Python 驱动，未知设备可适配** | **1–3 台** | **¥699–899** | **首批主力** |
| Gateway Station | 香橙派 3B / RDK X5 | 多设备网关 + 边缘自动化 | 4–10 台 | ¥1,499–1,999 | Phase2 起 |

> 首批仅生产 **Gateway Pro**：Python 驱动 100% 复用、无需 ESP32 固件开发、单一 SKU 降低供应链和测试复杂度。

### 1.5 两类用户与使用体验

产品要同时照顾两种截然不同的用户，他们的使用路径和期望完全不同。整体设计原则是：**默认路径对小白零门槛，进阶路径对开发者足够开放**。

#### 用户 A：不写代码的使用者（默认路径）

绝大多数用户属于这一类——他们只想让仪器的数据流起来、能远程看能远程控，完全不想碰代码。对他们来说，从拆箱到看到数据只有五步：

1. **上电**：网关插电开机；
2. **配网**：用手机 APP 通过蓝牙给网关配上实验室 WiFi（无需连数据线、无需登录路由器）；
3. **配实验室**：在 leap-lab 前端建好实验室并与这台网关绑定（一次性动作）；
4. **插设备**：转接线一头插网关、一头插仪器；
5. **看数/控制**：等十几秒到半分钟，仪器自动出现在前端，数据开始上报，也能从前端下发控制指令。

全程**不需要**写代码、选波特率、编辑配置、装任何软件。设备能不能被自动识别，取决于网关内置指纹库是否已收录该型号——这正是用户 B 要帮忙扩充的地方。

**每一步的风险点（决定"开箱即用"能否真正成立）：**

| 步骤 | 主要风险 | 现状 / 对策 |
|---|---|---|
| ① 上电 | 断电频繁的实验室里，异常掉电可能损坏文件系统 → 变砖 | 需 overlayfs 只读根 + 数据分区分离（量产验收含"50 次强制断电无损坏"） |
| ② 配网 | **当前 AP/STA 切换偶发 WiFi 芯片卡死**；金属机柜/校园网隔离导致连不上；配网页体验差 | 用蓝牙配网彻底替代 AP（阶段 A 第一优先级）；备选 USB 以太网 / 外置天线 / 自建 AP 子网 |
| ③ 配实验室 | 唯一需要用户"动脑"的一步：建实验室 + 绑定网关；克隆机身份冲突（machine-id / hostname / 云 UUID 重复）会导致绑错设备 | 母机首启脚本自动重生成唯一身份（已验证）；SN 码/二维码辅助绑定，减少手输出错 |
| ④ 插设备 | 转接线型号不匹配（RS232/RS485/USB 混插）；USB 自定义 VID:PID 在 Linux 不生成串口节点（如 Zolix FTDI D493）；热插拔 fd 失效 | 随附标注好的转接线；常见自定义 PID 预置 udev 规则；DeviceWorker 已做热插拔重连兜底 |
| ⑤ 看数/控制 | **设备未被指纹库收录 → 识别不到**（最大信任杀手）；探测串行耗时随设备数线性增长；二进制探测污染后续 ASCII 设备缓冲（Zolix E01）；长阻塞动作超时 | 指纹库靠用户 B 持续扩充；VID:PID 预筛 + 并行探测 + 探测前缀冲缓冲（阶段 C）；识别不到时提供"透传/手动确认"兜底而非直接失败 |

> 一句话：这条路径最脆弱的是 **②配网** 和 **⑤识别**——前者靠蓝牙配网收口，后者靠指纹库增长 + 半自动兜底收口，二者都是量产前必须过关的点。

#### 用户 B：有开发能力的贡献者（进阶路径）

少数有开发能力的用户，手上的设备可能还没被指纹库收录，他们希望能**自己写驱动、并让网关支持更多设备**。前面反复提到的"桌面安装包"就是为这类用户设计的：

- **这个安装包本质上是 unilabos 的安装包**，内置在网关里。用户可以用它在**自己的电脑**上一键安装 unilab 开发环境；
- 在本机环境里为自己的设备**编写/调试驱动**（复用现有 `@device` / `@action` / 指纹库那套机制）；
- 驱动写好后**上传到"设备广场"**，从而丰富网关能自动识别与支持的设备型号；
- 一旦某个驱动进入设备广场，**所有网关都能通过指纹库 + 自动下载机制受益**——这就是指纹库"越用越全"的增长飞轮：每多一个贡献者，用户 A 那边就多一批开箱即用的设备。

> 简言之：用户 A 消费"开箱即用"，用户 B 生产"开箱即用"。桌面安装包是连接两类用户的桥梁——它把 unilab 开发能力下放给有能力的用户，再把成果通过设备广场回流给所有人。

---

## 二、系统架构概览

### 2.1 整体分层

网关是整个 Uni-Lab 系统里的一个**轻量边缘节点**——它省略了 ROS2 层和 HostNode，只保留「驱动层 + WebSocket 客户端」。在云端 Schedule Server 看来，网关和完整的 Uni-Lab-OS 边缘实例都是 Edge 连接，通过同一套 `EdgeImpl` 管理。

```
┌─────────────────────────────────────────────────────────┐
│  Uni-Lab Go 云后端（双进程 + Redis）                       │
│  ├─ API Server (80)：REST / Material WS / Workflow WS / SSE │
│  └─ Schedule Server (81)：Edge WS (/ws/schedule) / DAG 引擎  │
└───────────────┬───────────────────────┬───────────────────┘
   HTTP 注册资源树 │           实时交互 WebSocket │ 认证 Lab {base64(ak:sk)}
                 │                       │  消息 {"action","data","edge_session"}
        ┌────────┴────────┐     ┌────────┴─────────┐
        │  React 前端/APP  │     │  物联网网关       │
        │  设备广场/数据面板 │     │ (香橙派 Zero 2W) │
        └─────────────────┘     └──┬────┬────┬──────┘
                                  仪器A 仪器B 仪器C（串口/USB）
```

### 2.2 网关与云端的两条通道

- **HTTP 注册**：网关启动时向云端注册资源树（host_node + 设备列表），云端据此建设备树；
- **WebSocket 实时交互**：遥测上报（device_status）、任务执行（job_start / job_status）、忙闲查询（query/report_action_state）、心跳（ping/pong）。协议与现有 Uni-Lab-OS 完全一致，复用云端已有 handler。

### 2.3 即插即用引擎（核心竞争力）

```
USB 插入 → udev 事件（拿到 VID:PID）→ 指纹库逐条探测 → 命中
  → 自动下载对应驱动包（driver_url，按版本比对更新）
  → 实例化驱动 + 构建 worker → HTTP 注册 + WS 上报 → 前端可见可控
```

- **探测安全原则**：只发只读查询命令，不触发仪器动作；所有探测均有超时保护；
- **识别分级**：★★★★★ 指纹精确命中（完整能力）／★★★☆☆ 协议族匹配（通用字段）／★☆☆☆☆ 透传（原始 hex 上云分析）；
- **指纹库增长飞轮**：每新增一个 Python 驱动 → 提取指纹 → 写入指纹库 → 更多设备开箱即用。

---

## 三、已实现的能力（现状盘点）

> 截至 2026-07-09，以下能力已在真实香橙派网关上跑通。

### 3.1 网关运行时

- 完全独立于 ROS2 运行：WebSocket 客户端 + HTTP 资源树注册 + systemd 开机自启；
- 纯 Python 模式构建注册表（无 rclpy / unilabos_msgs 依赖），AST 扫描设备驱动生成 schema；
- 断线重连（固定 5s 间隔），重连后重新注册；WiFi 断开→本地缓存→恢复自动重传；
- 网关健康检查：以 `hardware_interface` 约定（连上=串口对象 / 掉线=端口字符串）判断设备在线状态，拔出设备 30s 后判定下线，再插入其他设备自动识别；
- `_build_workers` 具备兜底：单个驱动加载/实例化失败只跳过该设备，不拖垮整台网关。

### 3.2 即插即用与自动下载驱动

- USB 热插拔检测（udev）→ 指纹库匹配 → 自动注册到云端；
- **驱动按需下载**：指纹库条目配 `driver_url`（指向 `.tar.gz` 包），本地无此驱动时自动下载解压到 `unilabos/devices/`，`importlib` 直接加载；
- **驱动版本管理**：条目配 `driver_version`，与本地包 `pyproject.toml` 版本比对，不一致自动拉取新版（避免手动升级）；
- 指纹库支持 send/expect_prefix/expect_regex 多种匹配，二进制与 ASCII 协议均可探测。

### 3.3 已接入设备清单

| 设备 | 协议 | 备注 |
|---|---|---|
| 润泽注射泵 SY-03B | RS485 自定义 | 种子设备 |
| 电导 / 温度 / pH 传感器 | Modbus / RS485 / RS232 | |
| 步进电机 | RS485 驱控一体 | |
| 磁力搅拌仪（IKA / 大龙） | 串口 ASCII | |
| 润泽 SV07M 多通道切换阀 | RUNZE 二进制 | 本阶段接入 |
| ESP32 五路电磁阀控制器 | 串口 ASCII（PING 握手） | 本阶段接入 |
| 兰格 BT100-2J 蠕动泵 | 兰格二进制（1200 8E1） | 本阶段接入，含真心跳 |
| Zolix / Andor Omni-λ 单色仪（SR-500i） | ASCII（19200 8N1，SYSTEMINFO 探测） | 本阶段接入，含 FTDI 自定义 PID 处理 |

### 3.4 配网与身份

- **WiFi AP 配网向导**：上电→检测无网络→进入 AP 模式→手机连 `Unilab-gateway`→浏览器进配网页→选 WiFi 填密码。⚠️ 已知问题：AP↔STA 切换时 WiFi 芯片偶发卡死（重启可恢复），**计划由蓝牙配网取代**；
- **mDNS 配置入口**：`http://unilab-gateway.local/`，可改 AKSK / 重新配网；
- **母机克隆量产流程**：已梳理唯一身份要素（hostname / machine-id / SSH host key / 云端 UUID），克隆前需清理并首启重生成。

### 3.5 OTA 与移动端

- **TB 远程固件升级已跑通**：基于 ThingsBoard 的固件下发（当前依赖 TB，**计划改为自部署**）；
- **移动端 APP 已开发完成**（iOS + Android），待接入蓝牙配网。

---

## 四、待完成工作分解

### ① 蓝牙配网（会议定的第一优先级）

- **网关侧**：香橙派起 BLE GATT 外设服务（WiFi 扫描 / 凭据写入 / 状态回读特征值），复用现有 `wifi_manager`；用蓝牙**彻底绕开 AP/STA 切换卡死 bug**；
- **APP 侧**：APP 已完成主体，需补 BLE 配对 + WiFi 凭据下发流程；串联"加硬件带 SN 码 → 后端写新 AKSK → 提测"（SN 码正确即可直连服务端）；
- **联调**：APP ↔ 网关端到端配网，含 SN 码开局。

### ② 网关 Agent + 桌面安装包（重点）

- **网关内常驻 Agent**：一个跑在香橙派上的管理进程，负责运行时管理、固件/驱动升级、状态上报等。会议结论：Skill 放本地更贴合实际（读写路径本地化，避免 MCP 加载问题）；Runner 只是给 agent 的窗口，Skill 挂在 agent 上；
- **桌面安装包（unilabos 安装包，面向用户 B）**：网关内置一个 **unilabos 安装包**，供有开发能力的用户下载到**自己的电脑**上一键安装 unilab 开发环境（见 §1.5 用户 B 路径）。用户在本机为自己的设备编写/调试驱动，再上传到**设备广场**，从而扩充网关能自动识别的设备型号。它服务的是"让用户贡献驱动"这条飞轮，而非给终端用户装网关运行时。（注：架构文档 Phase3 另有一个 Tauri 桌面管理 App，用于图形化管理本地进程，属另一条线，不在本安装包范围内。）

### ③ 自建 OTA，替代 TB（重点）

- 把固件版本管理 + 分发 + A/B 分区 + 失败回滚从 TB 迁移到**自部署**（Uni-Lab Go 后端 OTA 模块）；
- 覆盖架构文档定义的 4 个场景：
  1. rootfs 整包（~3–5GB）下发 → A/B 分区写 inactive + reboot 切换 + 失败回滚；
  2. 纯 Python 代码增量 patch（~10–50MB）→ push 后 `systemctl restart unilab-gateway`；
  3. 单驱动 Python 包 + 指纹库条目下发到已售网关；
  4. 升级过程可观测：每台网关当前版本、升级到第几步、进度。

### ④ 收尾 / 优化项

- **指纹库优化**：设备变多后串行探测耗时线性增长（当前单口 10 条指纹已约 18s）+ 指纹重合风险 → 用 USB VID:PID 做第一层预筛 + 并行探测 + 探测命令前缀冲缓冲（避免前序二进制探测污染后续 ASCII 设备，如 Zolix E01 问题）；
- **母机镜像固化** + 首启唯一身份脚本 + 打标签流程（短码 = SoC serial 后 4 位 / AP 名 / 二维码）；
- **硬件**：外壳重做 + 灌胶工艺 + 转接线批量（USB-B / DB9-RS232 / RJ45-RS485）；
- **业务**：产品命名定稿、定价、玻尔付费方式对接（加密狗机制、统一扣年费）。

---

## 五、分期实施计划

> 以 2026-07-09 为起点。角色代号：E = 边缘端（Python）、H = 嵌入式/硬件、F = 前后端（Go/React/APP）。

```
        7月               8月                 9月              10月
   W2  W3  W4 │ W1  W2  W3  W4 │ W1  W2  W3  W4 │ W1  ...
   ── 阶段A ── │                │                │
   ─────── 阶段B ──────────── │                │
        ────────── 阶段C ─────────── │          │
                   ───────────── 阶段D ──────────────→ 量产
```

### 阶段 A — 蓝牙配网闭环（约 2.5 周，7/10–7/28）

| 负责 | 任务 |
|---|---|
| E | 网关 BLE GATT 服务（bluez / bless）：WiFi 扫描、凭据写入、状态回读特征值；复用 `wifi_manager` |
| E | SN 码 → 后端写新 AKSK 流程；蓝牙配网与现有配网并存，逐步替代 AP |
| F / 移动 | APP 补 BLE 配对 + WiFi 凭据下发 UI；与后端 SN→AKSK 提测打通 |
| E + 移动 | 端到端联调：开箱 → 蓝牙连 → 填 WiFi → 上线（目标 <5 分钟） |
| 🏁 里程碑 | **蓝牙配网替代 AP 配网，AP/STA 卡死问题归零** |

### 阶段 B — Agent + 桌面安装包（约 3.5 周，7/15–8/8，与 A 部分并行）

| 负责 | 任务 |
|---|---|
| E | 网关常驻 Agent（运行时管理 + 升级 + 状态上报；本地 Skill / Runner，MCP 本地路径） |
| E / F | 网关内置 unilabos 桌面安装包（面向用户 B），下载到本机一键装好开发环境，先出可测试版 |
| E / F | 设备广场驱动上传链路：本机写好驱动 → 上传 → 进指纹库 → 全网关自动下载受益 |
| 🏁 里程碑 | **开发者可用安装包在自己 PC 装 unilab 并成功上传一个驱动到设备广场** |

### 阶段 C — 自建 OTA 替代 TB（约 4 周，7/28–8/25，与 B 尾部并行）

| 负责 | 任务 |
|---|---|
| F | Go 后端 OTA 模块：固件版本库 + 下发通知 + 升级状态跟踪（替代 TB OTA API） |
| E | 网关侧 OTA agent 对接自建端点；A/B 分区 + reboot 切换 + 失败自动回滚 |
| E | 纯 Python 增量 patch + 单驱动/指纹条目下发（复用现有 `driver_fetch` 机制） |
| E | 指纹库优化：VID:PID 预筛 + 并行探测 + 探测前缀冲缓冲 |
| 🏁 里程碑 | **脱离 TB，自建 OTA 升级成功率可观测、可回滚** |

### 阶段 D — 量产准备（约 6 周，8/11–9/22，硬件为主）

| 负责 | 任务 |
|---|---|
| H | 外壳重做 + 灌胶工艺；转接线批量（USB-B / DB9-RS232 / RJ45-RS485） |
| E / H | 母机镜像固化 + 首启唯一身份脚本 + 打标签流程（短码 / AP 名 / 二维码） |
| H | 首批 50 套试产 → 质检 / 老化流程 |
| 产品 / 运营 | 命名定稿、定价、玻尔付费（加密狗机制）对接 |
| 🏁 里程碑 | **50 套试产交付 + 小范围内测** |

### → 量产（Phase3，9 月底起持续）

- 注塑开模、供应链批量采购、单批 100 套；
- 验收标准：良品率 >95%、OTA 升级成功率 >99%（失败自动回滚）、72 小时连续运行无故障、50 次强制断电系统无损坏（overlayfs）。

---

## 六、关键依赖与风险

| # | 风险 / 依赖 | 影响 | 对策 |
|---|---|---|---|
| R1 | **蓝牙配网是量产前置** | 配网体验不过关，出货全卡住 | 阶段 A 最先啃；APP 侧同步确认已含 BLE 流程 |
| R2 | **自建 OTA 与量产强相关** | 出货后每台还要管 TB token（已踩坑） | 出货前必须脱离 TB（阶段 C） |
| R3 | **指纹库扩张导致探测耗时/误撞** | 设备越多识别越慢、易误判 | VID:PID 预筛 + 并行探测 + 前缀冲缓冲 |
| R4 | **母机唯一身份** | 克隆后 machine-id / hostname / ssh key / 云 UUID 冲突 | 首启脚本自动重生成（方案已验证） |
| R5 | 串口协议千差万别，识别覆盖率不足 | "插上不认"损伤信任 | 半自动兜底（扫描+推荐+手动确认）；优先覆盖 Top20 高频设备 |
| R6 | WiFi 在金属机柜密集实验室信号差 / 校园网隔离 | 掉线、连不上云 | USB 以太网转接 / 外置天线 / 自建 AP 子网 |
| R7 | 命名 / 定价 / 付费未定 | 影响出货 | 非工程阻塞项，并行推进不占关键路径 |

---

## 七、里程碑一览

| 时间点 | 里程碑 | 前置条件 |
|---|---|---|
| 7/28 | 蓝牙配网闭环，AP 卡死归零 | 网关 BLE 服务 + APP BLE 流程 |
| 8/8 | unilabos 桌面安装包可测试 + 设备广场上传链路 | 打包 + 上传链路 |
| 8/25 | 自建 OTA 脱离 TB，可回滚可观测 | Go OTA 模块 + 网关 A/B 分区 |
| 9/22 | 首批 50 套试产 + 小范围内测 | 外壳/灌胶/转接线 + 母机固化 + 命名定价 |
| 10 月起 | Pro 量产（单批 100 套） | 注塑开模 + 供应链批采 |

---

## 八、按客户使用流程的功能交付时间表与兜底方案

> 从用户实际使用动作出发，逐个流程节点列出：**依赖哪个功能、目标交付时间、若延期的兜底方案、兜底状态下的体验代价**。
> 目的：确保任何一环延期，出货/试用都不会被完全卡死——都有一个"打折但可用"的退路。

### 8.1 用户 A（不写代码的使用者）主流程

| # | 客户动作 | 依赖功能 | 目标时间 | 负责 | 若延期的兜底方案 | 兜底体验代价 |
|---|---|---|---|---|---|---|
| 1 | 上电开机 | 母机镜像 + 断电保护(overlayfs) | 9/22（随试产） | E/H | 用当前已跑通的镜像手工烧卡；断电保护暂缺则贴"正常关机"提示 | 有异常掉电变砖风险，需人工重烧 |
| 2 | 配网 | **蓝牙配网**（APP+网关 BLE） | 7/28 | E+移动 | **保留现有 AP 配网**（`Unilab-gateway` 热点+配网页）；或 USB 有线直连 | 回到 AP/STA 偶发卡死；配网步骤多、体验差，但能连上 |
| 3 | 配实验室并绑定 | 前端绑定 + 唯一身份 + SN/二维码标签 | 9/22（标签随试产） | E/F/H | 唯一身份脚本已验证可用；标签未就绪时手工记录短码/UUID 绑定 | 需人工核对短码，易出错、绑定慢 |
| 4 | 插设备识别 | 指纹库 + udev + 自动下载驱动（**已实现**）；探测提速=VID:PID预筛+并行(阶段C) | 已可用 / 提速 8/25 | E | 提速未完成则维持串行探测（单口~18s，可接受）；型号未收录见下方"识别兜底" | 识别偏慢；多设备同插要排队 |
| 5 | 看数 / 远程控制 | 网关运行时（**已实现**） | 已可用 | E | — | 无（基础能力已具备） |
| ★ | 设备未被指纹库收录 | 半自动识别兜底（扫描+推荐+手动确认+透传上云） | 与阶段 C 同步 | E | 短期：让用户 B 补驱动进设备广场；再不行走**原始 hex 透传**，数据先上云 | 无完整语义，只能看原始数据、暂不能结构化控制 |
| ★ | 远程运维（升级/状态上报/故障排查） | **网关常驻 Agent**（升级执行+状态回传+本地 Skill/Runner） | 8/8 | E | 无 Agent 则靠 SSH 人工登录排查、手工重启服务；状态只能看云端遥测 | 不可远程自助运维，出问题要工程师逐台 SSH |
| ★ | 远程升级固件/驱动 | **自建 OTA**（下发/版本管理，由 Agent 在网关侧执行） | 8/25 | E/F | **继续用现有 TB OTA**；或人工 `scp` + `systemctl restart` | TB 需逐台管 token（已知痛点）；人工升级不可批量 |

### 8.2 用户 B（开发者贡献驱动）路径

| # | 客户动作 | 依赖功能 | 目标时间 | 负责 | 若延期的兜底方案 | 兜底体验代价 |
|---|---|---|---|---|---|---|
| 1 | 在本机装 unilab 开发环境 | **网关内置 unilabos 桌面安装包** | 8/8 | E/F | 走现有方式：`git clone` + `pip install -e .` 手工装 | 门槛高，只有懂命令行的人能上手 |
| 2 | 写 / 调试新设备驱动 | `@device`/`@action` + 指纹库机制（**已实现**） | 已可用 | E | — | 无（机制已在，本文档接入的 4 个设备即样例） |
| 3 | 上传驱动到设备广场 | **设备广场上传链路** | 8/8 | E/F | 手工提 PR 到仓库 / 人工挂 `driver_url` 到指纹库 | 需维护者介入，回流慢、不自助 |
| 4 | 新驱动惠及所有网关 | 指纹库 + 自动下载（**已实现**） | 已可用 | E | — | 无（`driver_url`+`driver_version` 机制已跑通） |

### 8.3 关键结论

- **完全不阻塞出货的环节**：④识别、⑤看数控制、用户 B 的②④——这些核心能力**现在就已具备**，即便所有新功能延期也能演示/试用；
- **有优雅降级、可延期的环节**：②配网（退回 AP）、常驻 Agent 远程运维（退回 SSH 人工）、远程升级（退回 TB）、桌面安装包（退回手工装）、上传链路（退回提 PR）——延期只是**体验打折**，不致命；
- **真正的硬约束**（延期即卡量产，不可降级）：
  - **量产前必须完成蓝牙配网（7/28）**——AP 卡死是硬伤，不能带病出厂；
  - **量产前必须脱离 TB（8/25）**——否则每台设备的 token 运维不可持续；
  - **母机唯一身份（已验证）**——克隆冲突是致命的，好在方案已就绪。

> 换句话说：**能演示的功能大都已就位，剩下的工作重点是把"能用"打磨成"量产可交付"**，而其中只有「蓝牙配网」和「自建 OTA」两项是不可绕过的量产闸门，其余均有兜底退路。

---

*本文档基于当前进度整理，随开发推进持续更新。*
